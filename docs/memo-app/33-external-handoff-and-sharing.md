# 外部AI・共有受け渡し（Phase 16C）

追加日：2026-07-23  
文書状態：承認済み仕様／実装済み

対象Phase：Phase 16C  
対象リポジトリ：`ykjob/MindHub_App`

実装状態（2026-08-02）：**実装済み・Web確認済み。Codex初回レビューの指摘（Medium 4・Low 4）を修正済みで、Codex再レビュー待ち・EAS APK更新待ち・更新APKでのPixel最終確認待ち**。詳細は §20（実装記録）・§21（レビュー対応記録）。§1〜§19の仕様内容は実装・レビュー対応によって変更していない。

## 1. 文書の位置づけ

本ファイルは、MindHub内のプロンプト、さくっとメモ、現場適応の生成文を、ユーザー確認後にクリップボードまたはOS共有機能へ渡す共通仕様の正本とする。

既存仕様との分担は以下とする。

| 内容 | 正本 |
|---|---|
| ChatGPT整理プロンプト本文 | `06-chatgpt-prompt-copy.md` |
| プロンプト集の収録内容・分類 | `14-mobile-prompt-hub-and-inbox.md`および実装上のプロンプト定義 |
| さくっとメモ本文・保存・GitHub | `docs/flowdock_mvp_design.md` |
| 現場適応の質問・報告・守秘 | `21`〜`23` |
| コピー、共有確認、OS共有、Web代替、共有前保存 | 本ファイル |

本機能はOpenAI API、ChatGPT API、Slack APIを利用する直接連携ではない。

## 2. 背景

現行MindHubでは、プロンプト集、さくっとメモ本文、現場適応の質問・報告などをクリップボードへコピーできる。

コピーは、既存会話へ貼る、一部分だけ利用する、PCへ渡す等に必要であり、廃止しない。一方、スマートフォンでは次の手順が負担になる。

1. コピー
2. ChatGPT等のアプリを探して開く
3. 入力欄へ貼り付ける
4. 必要に応じて元メモも追加する

Phase 16Cでは、コピーに加えて`ChatGPTなどへ共有`を追加し、OSの共有画面からChatGPT、Gemini、Slack、メール、メモアプリ等をユーザーが選択できるようにする。

## 3. 確定した製品判断

- コピー機能を残す
- 共有ボタン名は`ChatGPTなどへ共有`
- 押下時はOSまたはブラウザの共有機能を開く
- ChatGPTだけへ直接送信しない
- 共有先はユーザーが選ぶ
- 共有前に全文を表示し、編集できる
- 使用場所ごとに共有内容の既定を決める
- 通常メモは注意文、現場適応は守秘チェック必須
- 共有用編集内容は元データを上書きしない
- 編集後の文章は、ユーザーが選べば新しい記録として保存できる
- 初期対象は次の4系統
  1. プロンプト集
  2. さくっとメモ詳細
  3. 現場適応の質問文
  4. 現場適応の進捗報告文

## 4. 目的

- コピー中心の手動操作を残しつつ、スマートフォンから外部アプリへ渡しやすくする
- 共有前に内容と守秘情報を確認できる
- プロンプトと対象本文を1つの文章にまとめられる
- MindHubから自動送信せず、最終操作をユーザーへ残す
- ChatGPTだけに依存しない汎用的な受け渡し機能にする
- WebとAndroidの双方で破綻しない

## 5. 対象外

- OpenAI API、ChatGPT API、Assistants API等の利用
- ChatGPTアプリ内部の特定会話を直接開く
- ChatGPTアプリだけを強制的に共有先指定する
- Slack API、Webhook、Botによる自動投稿
- Gemini API、Claude API等への自動送信
- 外部サービスの認証情報保存
- 共有先アプリのインストール確認
- 実際に相手へ送信されたことの保証
- 共有履歴、共有先履歴、送信済み状態
- 外部AIからの回答取り込み
- 複数メモの一括共有
- 添付ファイル、画像、音声の共有
- 自動マスキング・自動機密情報検出

## 6. 共通フロー

```text
対象画面
  ↓
［ChatGPTなどへ共有］
  ↓
外部で使う内容の確認画面
  ├─ 用途選択（さくっとメモのみ）
  ├─ 全文確認・編集
  ├─ 守秘注意／守秘チェック
  ├─ コピー
  ├─ ChatGPTなどへ共有
  ├─ 新しい記録として保存（任意）
  └─ キャンセル
  ↓
OSまたはブラウザの共有画面
  ↓
ユーザーが共有先を選択
```

MindHubは、共有画面を開いた後の送信操作を制御・保証しない。

## 7. 初期対象別仕様

### 7.1 プロンプト集

#### SHARE-PROMPT-01 共有内容

- 選択したプロンプト本文だけを共有確認画面へ渡す
- プロンプト名を確認画面上の出所情報として表示する
- 共有文章へプロンプトIDや内部管理情報を強制的に含めない

#### SHARE-PROMPT-02 操作

各プロンプトに次を表示する。

- `コピー`
- `ChatGPTなどへ共有`

既存の展開、検索、状況別絞り込み、42件の内容を変更しない。

#### SHARE-PROMPT-03 保存

編集したプロンプトを保存する場合、元のプロンプト定義を変更しない。

- `新しい記録として保存`は、既存`/notes/create`相当の記録作成へ編集文章を引き継ぐ
- ユーザーがタイトル・カテゴリ等を確認して保存する
- テンプレートDBやコード固定プロンプトを上書きしない

### 7.2 さくっとメモ詳細

#### SHARE-MEMO-01 入口

本文コピーを残し、その近くに`ChatGPTなどへ共有`を追加する。

本文が空の場合は共有不可とし、理由を文字で表示する。

#### SHARE-MEMO-02 用途選択

共有確認画面で次の5用途から選ぶ。

1. 内容を整理する
2. やることを抽出する
3. 質問内容を整理する
4. 開発依頼に整える
5. 自由に編集する

初期選択は`内容を整理する`。

用途は少数の入口であり、既存42件をすべて選択させない。

#### SHARE-MEMO-03 文章の組み立て

用途1〜4では、既存プロンプト定義を再利用し、次の構成で1つの文章を作る。

```text
【依頼内容】
＜選択用途に対応する既存プロンプト＞

【整理対象】
＜さくっとメモ本文＞
```

- 既存プロンプト本文を新しい場所へ複製して別管理しない
- 利用する既存プロンプトIDの具体的対応は、本文の意味を確認して実装時に固定マッピングする
- マッピングはコード側で明示し、名前の部分一致で自動選択しない

用途5では、メモ本文を初期表示し、ユーザーが依頼文を自由に追加・編集する。

#### SHARE-MEMO-04 元メモ非変更

共有確認画面で編集しても、`memos`の元本文を変更しない。

#### SHARE-MEMO-05 新規記録保存

- 編集文章を保存する場合は、既存`notes`の新規作成画面へ引き継ぐ
- 元のさくっとメモを上書きしない
- 保存操作を行わなければ一時文章として破棄する

### 7.3 現場適応・質問文

- `32-workplace-question-timing-rules.md`で生成した完成質問文を共有確認画面へ渡す
- プロンプトは追加せず、完成質問文だけを既定共有内容とする
- 全文編集可能
- 現場適応用守秘チェックを必須にする
- 新規記録保存は`workplace,workplace_question`、private、Git候補falseを強制する

### 7.4 現場適応・進捗報告文

- `buildReportText`等で生成した完成報告文を共有確認画面へ渡す
- プロンプトは追加せず、完成報告文だけを既定共有内容とする
- 全文編集可能
- 現場適応用守秘チェックを必須にする
- 保存する場合は既存notesを利用し、次を強制する
  - `type`: `thought`
  - `tags`: `workplace,workplace_report`
  - `visibility`: `private`
  - `is_git_candidate`: `false`
  - 一般的な日付入りタイトル

## 8. 共有確認画面

### 8.1 表示項目

- 画面タイトル：`外部で使う内容を確認`
- 出所
  - プロンプト名
  - さくっとメモ
  - 現場適応・質問
  - 現場適応・報告
- 用途選択（さくっとメモのみ）
- 編集可能な全文入力欄
- 通常注意または現場適応用守秘チェック
- コピー
- ChatGPTなどへ共有
- 新しい記録として保存
- キャンセル／戻る

### 8.2 全文入力欄

- 長文を内部スクロールできる
- 改行、日本語、記号を維持する
- 現在の編集位置がキーボードに隠れない
- 共有前に全文を確認できる
- 空文字の場合はコピー・共有・保存を無効にする

入力挙動は`31`の長文入力基準と整合させる。

### 8.3 通常注意

プロンプト集・さくっとメモでは、次の趣旨を表示する。

> 氏名、会社名、認証情報、非公開URLなど、外部サービスへ渡してはいけない情報が含まれていないか確認してください。

毎回のチェックボックスは必須にしない。

### 8.4 現場適応用守秘チェック

質問・報告では次の3項目を必須にする。

- [ ] 顧客名・会社名・個人名を含んでいない
- [ ] 認証情報・社内URL・内部システム名を含んでいない
- [ ] 会社固有の手順・判断基準・非公開情報を含んでいない

3項目すべてが確認されるまで`ChatGPTなどへ共有`を無効にする。

コピーについても外部持ち出しにつながるため、現場適応では同じチェック完了後に利用可能とする。

## 9. コピー

### SHARE-COPY-01 維持

既存コピー機能を削除・共有へ置換しない。

### SHARE-COPY-02 結果表示

- 成功：`コピーしました`
- 失敗：`コピーできませんでした`
- コピー中は二重実行を防止する
- 結果表示は一定時間後に通常状態へ戻す

### SHARE-COPY-03 コピー内容

画面に表示されている編集後全文と、クリップボード内容が一致すること。

## 10. OS・ブラウザ共有

### SHARE-OS-01 Android

- React Native／Expoで利用可能な標準共有機能を第一候補とする
- 新規依存を追加せず実現できる方法を優先する
- OSの共有先一覧を開く
- ユーザーがChatGPT等を選ぶ

### SHARE-OS-02 Web

- ブラウザ共有機能が利用可能な場合は共有画面を開く
- 利用できない場合はエラーで終了せず、次を案内する

> この環境では共有画面を開けないため、コピーして利用してください。

- コピーは引き続き利用可能にする

### SHARE-OS-03 iOS

- Phase 16Cの必須完了条件には含めない
- 標準共有機能で明らかな回帰を持ち込まない

### SHARE-OS-04 完了表示

MindHubが表示してよい表現：

- `共有画面を開きました`
- `共有を開始しました`
- `共有画面を開けませんでした`

表示してはいけない表現：

- `ChatGPTへ送信しました`
- `共有が完了しました`
- `相手に届きました`

OSやブラウザから明確なキャンセル結果を取得できない場合は、推測で成功扱いにしない。

## 11. 新しい記録として保存

### SHARE-SAVE-01 基本

- 共有用編集文章は一時データ
- 保存を選択した場合だけ、新しい`notes`レコードとして保存する
- 元のプロンプト、さくっとメモ、質問・報告の元データを上書きしない

### SHARE-SAVE-02 通常系

プロンプト集・さくっとメモからの保存は、既存の記録作成画面へ本文を引き継ぎ、ユーザーがタイトル・カテゴリ・タグ等を確認して保存する。

- 自動保存しない
- Git候補・visibilityは既存記録作成画面のルールに従う
- 引き継ぎ方法は実装判断とする

### SHARE-SAVE-03 現場適応系

質問・報告は、一般の記録作成画面へ渡して守秘既定を変更できる状態にせず、現場適応専用保存処理でprivate・Git候補falseを強制する。

### SHARE-SAVE-04 保存タイミング

`新しい記録として保存`は共有確認画面から利用できる。コピー・共有の前後を問わないが、保存は必須ではない。

## 12. 一時データ

- 共有確認画面を閉じ、保存していない編集文章は破棄してよい
- 共有履歴をDBへ残さない
- 共有先アプリ名を保存しない
- 共有日時を元メモへ記録しない
- 共有後にMindHubへ戻った場合、画面が生存している間は編集文章を維持してよい

## 13. ルート・画面構成

共有確認画面を共通ルートとして追加できる。

具体的なルート名は実装判断とするが、次を満たすこと。

- プロンプト集、さくっとメモ、質問、報告から再利用可能
- Web直アクセス時に空白・クラッシュ・機密データ表示を起こさない
- 共有対象をURLクエリへ全文で入れない
- 戻るフォールバックがある
- 一時文章を不用意にログへ出さない

## 14. データ・セキュリティ

- 共有本文をSQLiteへ自動保存しない
- URL、ログ、エラー、analyticsへ共有本文を出さない
- 認証情報を検出・送信する機能を作らない
- 外部アプリへ渡した後のデータ管理は共有先サービスの範囲であることを注意する
- 現場適応の守秘ルールは一般共有ルールより優先する
- GitHubアップロード、公開HTML、配布用出力とは別機能として扱う

## 15. エラー・例外処理

- 共有本文が空：コピー・共有・保存を無効化し理由表示
- 共有API利用不可：コピー案内
- 共有画面起動失敗：編集文章を維持し失敗表示
- 共有キャンセル：編集文章を維持し、エラー・送信済みにしない
- 保存失敗：編集文章を維持し再保存可能
- コピー失敗：共有は利用可能なまま
- 共有中の連打：多重起動防止
- 画面アンマウント後の状態更新・タイマー残留を防止

## 16. アクセシビリティ

- コピー、共有、保存、キャンセルは44相当
- 処理中、無効、結果を文字と`accessibilityState`で示す
- 守秘チェックの選択状態を読み上げ可能にする
- 共有確認全文をスクリーンリーダーで確認できる
- 最大フォントサイズでも共有・戻るへ到達できる

## 17. 受入条件

### 17.1 Gate A：プロンプト集

- [ ] 既存コピーが機能する
- [ ] 各プロンプトから共有確認へ進める
- [ ] 選択したプロンプト本文が欠けない
- [ ] 検索・絞り込み・展開・42件を壊していない

### 17.2 Gate B：さくっとメモ

- [ ] 5用途を選べる
- [ ] 既定は`内容を整理する`
- [ ] プロンプト＋本文を1つの文章として編集できる
- [ ] 編集しても元メモが変わらない
- [ ] 本文コピー、編集、削除、GitHub連携を壊していない

### 17.3 Gate C：現場適応

- [ ] 質問文・報告文を共有確認へ渡せる
- [ ] 3つの守秘チェック完了前はコピー・共有できない
- [ ] 保存時にprivate・Git候補falseが強制される
- [ ] 会社固有情報の入力を促す文言がない

### 17.4 Gate D：Android共有

- [ ] OS共有画面が開く
- [ ] ChatGPTが候補にある場合、ユーザーが選択できる
- [ ] ChatGPT未インストールでもクラッシュしない
- [ ] 共有キャンセル後にMindHubへ戻れる
- [ ] 長文・日本語・改行が欠けない
- [ ] 多重共有画面が開かない

### 17.5 Gate E：Web

- [ ] 共有対応ブラウザでは共有を開始できる
- [ ] 非対応ブラウザではコピー案内を表示する
- [ ] 360 / 390 / 412 / 480 / 768 / 1024幅で操作できる
- [ ] Web直アクセスで本文漏えい・空白画面・クラッシュがない

### 17.6 Gate F：保存

- [ ] 通常系は新しい記録作成へ引き継げる
- [ ] 現場適応系は専用安全既定で保存される
- [ ] 元データを上書きしない
- [ ] 保存しなければDBへ残らない
- [ ] 保存失敗後に再試行できる

### 17.7 Gate G：表示文言

- [ ] `ChatGPTへ送信しました`等の誤表示がない
- [ ] コピー、共有開始、保存を区別して表示する
- [ ] 共有先をMindHubが保証しないことが分かる

## 18. Claude Codeへ任せてよい判断

- React Native標準`Share`、Web Share API等の具体的利用方法
- 共通共有画面のroute名・component名・hook名
- 一時文章の安全な画面間引き継ぎ方法
- 既存プロンプトIDと5用途の固定マッピング
- Web共有可否の検出方法
- 共有起動中の状態管理
- 保存画面へのprefill方法
- 確認用テスト・ハーネス

ただし、既存プロンプトの意味を確認し、用途と合わないマッピングを行わないこと。判断理由と採用マッピングを実装報告に記載する。

任せない判断：

- コピー機能の削除
- ボタン名の変更
- API・Webhook・新規認証の追加
- 自動送信
- 共有履歴保存
- 現場適応の守秘チェック省略
- 元データ上書き
- 共有完了の断定
- 新規依存追加

## 19. 実装単位

### 16C-1 共通共有基盤

- 共有確認画面
- 編集・コピー・OS共有
- Web fallback
- 二重操作・エラー状態

### 16C-2 プロンプト集・さくっとメモ

- 共有入口
- 5用途
- プロンプト固定マッピング
- 通常注意
- 新規記録作成への引き継ぎ

### 16C-3 現場適応

- 現場適応の質問・報告の**共有接続**（16C-1の共通共有確認画面へ渡す）
- 必須守秘チェック（3項目完了前はコピー・共有できない。`23` §5.1）
- **報告文の任意保存**（現場適応専用保存処理で private・Git候補false 強制。`workplace,workplace_report`）
- Android実機・回帰確認

※**完成質問文の任意保存そのものは 16B-3**（`32` §20）が担当し、16C-3では質問・報告を共有確認画面へ接続する。同じ保存処理を16B-3と16C-3で二重実装しない（現場適応専用保存処理を共用する）。

## 20. 実装記録（2026-08-02）

16C-1／16C-2／16C-3を1バッチで実装した。§1〜§19の製品判断・受入条件は変更していない。

### 20.1 実装場所

| 役割 | ファイル |
|---|---|
| 共有確認画面（route `/share/confirm`） | `app/share/confirm.tsx`（新規） |
| 画面間の一時引き継ぎ（共有対象／記録作成prefill） | `src/features/share/shareHandoff.ts`（新規） |
| 5用途・プロンプト固定マッピング・共有文章の組み立て | `src/features/share/shareTargets.ts`（新規） |
| OS／ブラウザ共有の起動 | `src/utils/share.ts`（新規） |
| ルート登録（title・戻るフォールバック） | `app/_layout.tsx` |
| プロンプト集の共有導線 | `app/prompts/index.tsx` |
| さくっとメモ詳細の共有導線 | `app/memo/[id]/index.tsx` |
| 現場適応 質問・報告の共有導線 | `app/workplace/question.tsx`・`app/workplace/report.tsx` |
| 報告文の現場適応専用保存（`saveReportNote`） | `src/features/workplace/workplaceService.ts` |
| 記録作成へのprefill受け取り | `app/notes/create.tsx` |
| 保存ヒント文の上書き許可（onSaveなしの場合） | `src/components/WorkplaceSceneForm.tsx` |

DB・`schema.ts`・`migrations.ts`・新規npm依存・`app.json`・`eas.json`・versionCodeは変更していない。共有はReact Native標準 `Share`（native）とWeb Share API（web）だけで実現している。

### 20.2 5用途の固定マッピング（§7.2）

| 用途 | 依頼文の取得元 | 採用理由 |
|---|---|---|
| 内容を整理する | 既存プロンプト `thought`（`chatgptPrompts.ts` の思考メモ整理） | 「ダーッと書く／整理すると／決める必要があること」等の見出しを持ち、未整理のメモ本文を整理する目的に一致する |
| やることを抽出する | 既存プロンプト `brain_dump_to_action` | 「今日やる／後日やる／保留／やらなくていい／確認が必要」に分ける定義で、やること抽出に一致する |
| 質問内容を整理する | **共有専用の固定依頼文**（§20.3の例外） | 既存42件に質問内容を整理する意味の定義がないため |
| 開発依頼に整える | 既存プロンプト `claude_prompt`（Claude Code用プロンプトとして整理） | 作業目的・変更範囲・完了条件・やらないことを揃える定義で、開発依頼に整える目的に一致する |
| 自由に編集する | 依頼文なし（さくっとメモ本文のみ） | §7.2の定義どおり |

制約：

* 用途1・2・4は `getPromptGroups()` からIDで取得する。プロンプト本文を別の定数へ複製しない（42件との二重管理を作らない）
* マッピングはIDで固定し、名称の部分一致で自動選択しない
* 共有文章の構造は `【依頼内容】` ＋ `【整理対象】` で全用途共通（用途5は本文のみ）

### 20.3 用途3の例外（共有専用の固定依頼文）

* 用途1・2・4は**既存プロンプト定義を再利用**する
* 用途3は**既存42件に意味の合う定義がない**ため、共有文章の組み立て専用の**固定依頼文**を使う（実装：`src/features/share/shareTargets.ts` の `QUESTION_ORGANIZE_REQUEST`）
* この固定依頼文は次のいずれにも含めない：プロンプト集の42件／`MOBILE_PROMPTS`／`NOTE_CATEGORIES`／Prompt Hubの検索・分類／DBテンプレート（`note_templates`）／ユーザー編集可能なプロンプト／`35` の用途別プロンプト編集構想
* **意味の合わない既存プロンプトを流用しないための例外**であり、用途3を減らす・保留するための措置ではない
* 現場適応の質問構造（`21`〜`23`・`32`）を参考にした共有専用仕様であり、`buildQuestionText`（未記入欄を含む完成質問文の生成）は呼び出さない
* 新しいPhase番号は作らない

### 20.4 既存プロンプト末尾の入力待ち行

* 既存プロンプト定義・42件の内容・生成HTML（`scripts/generate_prompt_hub.mjs` の出力）は変更しない
* 共有文章の組み立て時のみ、本文末尾に**完全一致**する入力待ち行（`整理対象：` / `整理対象（説明文）：`）を表示用に取り除く
* 本文途中に現れる「整理対象」の記述は削除しない
* 除去後に共通形式の `【整理対象】` 見出しとさくっとメモ本文を付ける

### 20.5 守秘チェックの適用範囲（判断B・2026-08-02決定）

* 守秘3チェックを必須にするのは、**新設した共有確認画面内の「コピー」と「ChatGPTなどへ共有」**（現場適応系のみ）
* `/workplace/question`・`/workplace/report` の**既存出力コピーは現状のまま維持**し、守秘チェックを追加しない
* 理由：§8.4は共有確認画面の仕様として定義されている／既存コピー機能を壊さない（§3・§9 SHARE-COPY-01）／Phase 16Cでは新しい外部受け渡し導線に確認手順を追加する／既存画面には既に守秘注意文（`WORKPLACE_PRIVACY_NOTICE`）が常時表示されている
* `23` §5.1 の「コピーも無効にする」は、この共有確認画面内のコピーを指すものとして実装した

### 20.6 一時データの扱い（§12・§13）

* 共有対象・共有用編集文章・記録作成prefillは、いずれもモジュール変数のみで保持する（URLクエリ・route params・SQLite・ログ・エラー・analyticsへ出さない）
* 受け側は `useState` 初期化関数で **get（純粋な読み取り）**してstateへ取り込んだ後、初回commit後の `useEffect` で **clear** する（Strict Mode安全。stateへ取り込む前には消さない）
* Webのリロード後・直アクセスでは復元されない（共有確認画面は「共有する内容がありません」＋ホームへ戻るを表示する）
* OS共有画面から戻っただけでは編集中の文章を消さない（共有・コピー・保存はいずれも画面stateを保持する）
* 共有確認用（`SharePayload`）と記録作成prefill用（`NoteDraftHandoff`）は別スロット・別型・別関数名に分離し、混線しない

### 20.7 共有結果の表示（§10 SHARE-OS-04）

* 表示するのは `共有を開始しました`（処理中）／`共有画面を開きました`／`共有画面を開けませんでした`／`この環境では共有画面を開けないため、コピーして利用してください。` のみ
* Web Share APIの `AbortError`（キャンセル）・iOSの `dismissedAction` は、失敗表示にも送信済み表示にもしない（編集文章を維持したまま通常状態へ戻す）
* `共有が完了しました`／`ChatGPTへ送信しました`／`相手に届きました` は表示しない

### 20.8 検証状態（2026-08-02時点）

実施済み。

* `npx tsc --noEmit` 合格
* `npx expo export --platform web` 成功（バンドルエラー0。共有ディレクトリ `dist` が別プロセスに使用中だったため、出力先をリポジトリ外へ指定して実行）
* `npx expo-doctor` 17/18（失敗1件は既存の `expo 54.0.35` vs `~54.0.36` パッチ差＝本実装と無関係・未修正）
* `git diff --check`：**初回実装コミット（`5e76a29`）では末尾空白の通知1件**（本ファイル冒頭のMarkdownハード改行2スペース）。レビュー修正コミットで解消し、`git diff --check d1a0600..HEAD` は合格（§21.3）
* 用途マッピング・末尾行除去の単体確認（実モジュールをコンパイルしてNode実行）＝**32/32合格**（42件維持・ID重複なし・用途1/2/4が既存本文と一致・末尾行のみ除去・本文途中の記述は残る・用途3が固定依頼文で42件に含まれない・用途5は本文のみ・組み立て構造）
* Web確認（ヘッドレスChrome・専用ポート8117・使い捨てuser-data-dir・puppeteerはリポジトリ外scratchpad隔離）＝**82項目中80合格**。5用途と既定・共有文章の構造・元メモ非変更・空文字時の無効化と理由表示・共有結果4分岐（成功／キャンセル／失敗／非対応）・禁止文言なし・幅6種（360/390/412/480/768/1024）で横スクロールなし・記録作成prefillとリロード非復元・共有確認の直アクセス空状態と戻る・プロンプト集42件維持と共有導線・現場適応の守秘3チェック（未完了ではコピー／共有不可、3件完了で可）・報告文の専用保存（`/notes` 詳細で private・`workplace_report`・Git候補外）・質問画面の既存コピー／保存／完了導線の維持・console.error／pageerror／unhandledrejection 0件

未確認・未実施。

* **コピー成功経路**（ヘッドレスChromeが `clipboard-write` を拒否＝環境制約。失敗表示「コピーできませんでした」と一定時間後の通常状態復帰は確認済み。Phase 15と同じ既知事象）
* **Android実機確認（Expo Go）**：未実施
* **EAS APK更新**：未実施。新規ネイティブ依存はないため実装commit前のビルドは不要だが、**Phase 16Cのコード変更を配布APKへ反映するにはCodexレビュー合格後にPhase 16全体を含むEAS APK更新が必要**
* **更新APKでのPixel最終確認**：未実施。Expo Go確認は事前確認であり、最終確認は更新APKで行う
* `33` Gate A〜G のうち、Gate D（Android共有）は未確認。`32` Gate D（共有・守秘）もAPK確認後に判定する
* iOSは Phase 16Cの必須完了条件外（§10 SHARE-OS-03）

## 21. レビュー対応記録（Codex初回レビュー・2026-08-02）

対象コミット：`5e76a291a0eebad57b5e0285102ab3cedad7598b`（初回実装）。
初回レビュー結果：Critical 0／High 0／**Medium 4**／**Low 4**／Note 4／判定＝要修正・再レビュー。
対応：8件すべてを修正（初回コミットはamendせず、追加コミットで対応）。仕様（§1〜§19）の製品判断は変更していない。

### 21.1 Medium 4件

| # | 指摘 | 対応 |
|---|---|---|
| M1 | 用途選択で編集内容を確認なしに失う | 用途ごとに「生成直後の文章（baseline）」を保持し、`text !== baseline` で編集済みを判定。**同じ用途の再選択はno-op**、未編集なら即時変更、編集済みは既存の `confirmDialog`（Web＝`window.confirm`／Android＝`Alert.alert`）で「用途を変更／現在の編集内容は破棄されます」を確認。キャンセル時は用途・文章・各種結果表示をいずれも変更しない |
| M2 | 通常系保存の高速二重押下で `/notes/create` が複数pushされる | 共通hook `src/hooks/useNavigationLock.ts` を新設し、**同期refでロック**。順序は「ロック → `setNoteDraftHandoff` → `router.push`」。`router.push` が同期例外なら `clearNoteDraftHandoff()` してロック解除（本文はログ・表示へ出さない） |
| M3 | 4つの共有入口で遷移の二重操作防止がない | プロンプト集・さくっとメモ詳細・現場適応の質問／報告の4入口にも同じhookを適用。遷移中はボタンを無効化し `accessibilityState.disabled` / `busy` を設定。遷移が同期失敗した場合は `clearSharePayload()` |
| M4 | 現場適応保存後に文章を編集しても保存済み表示とdisabledが残る | 保存中は全文入力欄を `editable=false`（`accessibilityState.disabled`）にし、**保存開始時点の文章を保存処理へ渡す**（競合防止）。文章変更時は保存タイマーを解除して `idle` へ戻し、再保存できるようにした |

### 21.2 Low 4件

| # | 指摘 | 対応 |
|---|---|---|
| L5 | 文章変更後も以前のコピー成功表示が残る | `useCopyFeedback` に `reset()` と**世代（generation）管理**を追加。文章変更・用途変更確定時に `reset()` を呼ぶ。コピー処理中に文章が変わった場合、進行中の処理は中断しないが、完了時に世代が異なれば結果を表示せず `idle` へ戻す。処理中の二重コピー防止は維持。既存利用箇所は従来どおり（後方互換） |
| L6 | プロンプトID取得失敗時に無言で自由編集相当へ変わる | `shareTargets.ts` を結果型へ変更（`ShareRequestResult` / `ShareTextResult`＝`ok: true` と `ok: false, message` の判別可能ユニオン）。**用途5の「依頼文なし」と取得失敗を型で区別**。取得失敗時は本文だけへフォールバックせず、`選択した用途の文章を準備できませんでした。別の用途を選んでください。` を表示し、コピー・共有・保存を無効化。内部ID・本文はメッセージへ含めない。別用途を選べば復旧できる |
| L7 | `git diff --check` が非ゼロなのに文書では問題なしとしている | 本ファイル冒頭の変更行から末尾2スペースを外し、通常の空行で表示を維持。§20.8の記述を実際の結果（初回コミットで通知1件／レビュー修正で解消）へ訂正。`current-tasks.md`・`docs/worklog/current.md` も同様に訂正 |
| L8 | `workplaceTags.ts` の報告保存コメントが実装状態と一致しない | 「Phase 16C-3で追加予定」→「Phase 16C-3で実装済み」へ修正（定数値・タグ値は無変更） |

### 21.3 レビュー修正後の検証

* `npx tsc --noEmit` 合格
* `npx expo export --platform web` 成功（`dist` が別プロセス使用中のため出力先はリポジトリ外）
* `npx expo-doctor` 17/18（失敗1件は既存の `expo` パッチ差＝無関係）
* `git diff --check` 合格（作業ツリー・`d1a0600..HEAD` とも exit 0）
* 用途マッピング・末尾行除去・**ID取得失敗の結果型**の単体確認＝**43/43合格**（42件維持・ID重複なし・用途5の「依頼文なし」と取得失敗の区別・失敗メッセージに内部IDを含まない）
* Web確認（レビュー修正分。ヘッドレスChrome・専用ポート8117）＝**41/41合格**（同一用途の再タップno-op／未編集の即時変更／編集済みの確認表示・キャンセルで維持・確定で再生成／5経路の高速二重押下でpush1回・戻ると再操作可／保存中は入力不可・保存後の編集でidle復帰・再保存で新規レコード／文章変更でコピー・共有結果を解除・用途変更キャンセルでは解除しない）
* 初回実装分のWeb確認を回帰実行＝**83項目中81合格**（不合格2件はコピー成功経路＝ヘッドレスの `clipboard-write` 拒否という既知の環境制約）
* コピー成功経路は引き続き未確認（通常ブラウザ・実機で確認する）。ID取得失敗時の画面表示は、正常なマッピングでは発生しないため単体確認とコード上の無効化条件で担保している

### 21.4 レビュー対応後も残る状態

Codex**再レビュー待ち**／EAS APK更新待ち（Codexレビュー合格後にPhase 16全体を含む更新が必要）／Expo Go事前確認待ち／更新APKでのPixel最終確認待ち。**Phase 16Cは最終完了としない**。
