# 外部AI・共有受け渡し（Phase 16C）

追加日：2026-07-23  
文書状態：承認済み仕様／実装済み

対象Phase：Phase 16C  
対象リポジトリ：`ykjob/MindHub_App`

実装状態（2026-08-08）：**Phase 16C 最終完了**。コード実装済み・Web確認済み・**CodexレビューPASS**・**PR #3 で main 反映済み**（merge commit `b96cc669ddc7c33f3563070d85962a07bb3b85fd`。レビュー経緯は §21〜§23）。**Pixel＋Expo Go事前確認PASS**（2026-08-05。§23.6）。**EAS preview APK versionCode 10 が FINISHED**（Build ID `10bedf3b-31c3-4fa7-aefe-73f62f960b5e`／対象commit `8706d7d3fcbfa349a1b71c4d45bb65e7e61fbb0a`）で、**Pixelへ上書きインストールして最終受入PASS（2026-08-08。K1〜K22。§23.7）**。**§17 Gate A〜G すべてPASS**、**`32` Gate D もPASS**。

詳細は §20（初回実装）・§21（初回レビュー対応）・§22（再レビュー対応）・§23（再々レビュー結果と文書修正）。当初の製品判断・責務分担は維持しており、Codex再レビュー対応として、守秘3チェックを編集後の最終文章へ適用する安全補強を §8.4.1 に追加した（保存を守秘チェック必須へ拡張する等の製品範囲変更はしていない）。

## 1. 文書の位置づけ

本ファイルは、MindHub内のプロンプト、さくっとメモ、現場適応の生成文を、ユーザー確認後にクリップボードまたはOS共有機能へ渡す共通仕様の正本とする。

既存仕様との分担は以下とする。

| 内容 | 正本 |
|---|---|
| ChatGPT整理プロンプト本文 | `06-chatgpt-prompt-copy.md` |
| プロンプト集の収録内容・分類 | `14-mobile-prompt-hub-and-inbox.md`および実装上のプロンプト定義 |
| さくっとメモ本文・保存・GitHub | `docs/flowdock_mvp_design.md` |
| 現場適応の質問・報告・守秘 | `21`〜`23` |
| コピー、共有確認、OS共有、Web代替、共有前保存 | 本ファイル |

本機能はOpenAI API、ChatGPT API、Slack APIを利用する直接連携ではない。

## 2. 背景

現行MindHubでは、プロンプト集、さくっとメモ本文、現場適応の質問・報告などをクリップボードへコピーできる。

コピーは、既存会話へ貼る、一部分だけ利用する、PCへ渡す等に必要であり、廃止しない。一方、スマートフォンでは次の手順が負担になる。

1. コピー
2. ChatGPT等のアプリを探して開く
3. 入力欄へ貼り付ける
4. 必要に応じて元メモも追加する

Phase 16Cでは、コピーに加えて`ChatGPTなどへ共有`を追加し、OSの共有画面からChatGPT、Gemini、Slack、メール、メモアプリ等をユーザーが選択できるようにする。

## 3. 確定した製品判断

- コピー機能を残す
- 共有ボタン名は`ChatGPTなどへ共有`
- 押下時はOSまたはブラウザの共有機能を開く
- ChatGPTだけへ直接送信しない
- 共有先はユーザーが選ぶ
- 共有前に全文を表示し、編集できる
- 使用場所ごとに共有内容の既定を決める
- 通常メモは注意文、現場適応は守秘チェック必須
- 共有用編集内容は元データを上書きしない
- 編集後の文章は、ユーザーが選べば新しい記録として保存できる
- 初期対象は次の4系統
  1. プロンプト集
  2. さくっとメモ詳細
  3. 現場適応の質問文
  4. 現場適応の進捗報告文

## 4. 目的

- コピー中心の手動操作を残しつつ、スマートフォンから外部アプリへ渡しやすくする
- 共有前に内容と守秘情報を確認できる
- プロンプトと対象本文を1つの文章にまとめられる
- MindHubから自動送信せず、最終操作をユーザーへ残す
- ChatGPTだけに依存しない汎用的な受け渡し機能にする
- WebとAndroidの双方で破綻しない

## 5. 対象外

- OpenAI API、ChatGPT API、Assistants API等の利用
- ChatGPTアプリ内部の特定会話を直接開く
- ChatGPTアプリだけを強制的に共有先指定する
- Slack API、Webhook、Botによる自動投稿
- Gemini API、Claude API等への自動送信
- 外部サービスの認証情報保存
- 共有先アプリのインストール確認
- 実際に相手へ送信されたことの保証
- 共有履歴、共有先履歴、送信済み状態
- 外部AIからの回答取り込み
- 複数メモの一括共有
- 添付ファイル、画像、音声の共有
- 自動マスキング・自動機密情報検出

## 6. 共通フロー

```text
対象画面
  ↓
［ChatGPTなどへ共有］
  ↓
外部で使う内容の確認画面
  ├─ 用途選択（さくっとメモのみ）
  ├─ 全文確認・編集
  ├─ 守秘注意／守秘チェック
  ├─ コピー
  ├─ ChatGPTなどへ共有
  ├─ 新しい記録として保存（任意）
  └─ キャンセル
  ↓
OSまたはブラウザの共有画面
  ↓
ユーザーが共有先を選択
```

MindHubは、共有画面を開いた後の送信操作を制御・保証しない。

## 7. 初期対象別仕様

### 7.1 プロンプト集

#### SHARE-PROMPT-01 共有内容

- 選択したプロンプト本文だけを共有確認画面へ渡す
- プロンプト名を確認画面上の出所情報として表示する
- 共有文章へプロンプトIDや内部管理情報を強制的に含めない

#### SHARE-PROMPT-02 操作

各プロンプトに次を表示する。

- `コピー`
- `ChatGPTなどへ共有`

既存の展開、検索、状況別絞り込み、42件の内容を変更しない。

#### SHARE-PROMPT-03 保存

編集したプロンプトを保存する場合、元のプロンプト定義を変更しない。

- `新しい記録として保存`は、既存`/notes/create`相当の記録作成へ編集文章を引き継ぐ
- ユーザーがタイトル・カテゴリ等を確認して保存する
- テンプレートDBやコード固定プロンプトを上書きしない

### 7.2 さくっとメモ詳細

#### SHARE-MEMO-01 入口

本文コピーを残し、その近くに`ChatGPTなどへ共有`を追加する。

本文が空の場合は共有不可とし、理由を文字で表示する。

#### SHARE-MEMO-02 用途選択

共有確認画面で次の5用途から選ぶ。

1. 内容を整理する
2. やることを抽出する
3. 質問内容を整理する
4. 開発依頼に整える
5. 自由に編集する

初期選択は`内容を整理する`。

用途は少数の入口であり、既存42件をすべて選択させない。

#### SHARE-MEMO-03 文章の組み立て

用途1〜4では、既存プロンプト定義を再利用し、次の構成で1つの文章を作る。

```text
【依頼内容】
＜選択用途に対応する既存プロンプト＞

【整理対象】
＜さくっとメモ本文＞
```

- 既存プロンプト本文を新しい場所へ複製して別管理しない
- 利用する既存プロンプトIDの具体的対応は、本文の意味を確認して実装時に固定マッピングする
- マッピングはコード側で明示し、名前の部分一致で自動選択しない

用途5では、メモ本文を初期表示し、ユーザーが依頼文を自由に追加・編集する。

#### SHARE-MEMO-04 元メモ非変更

共有確認画面で編集しても、`memos`の元本文を変更しない。

#### SHARE-MEMO-05 新規記録保存

- 編集文章を保存する場合は、既存`notes`の新規作成画面へ引き継ぐ
- 元のさくっとメモを上書きしない
- 保存操作を行わなければ一時文章として破棄する

### 7.3 現場適応・質問文

- `32-workplace-question-timing-rules.md`で生成した完成質問文を共有確認画面へ渡す
- プロンプトは追加せず、完成質問文だけを既定共有内容とする
- 全文編集可能
- 現場適応用守秘チェックを必須にする
- 新規記録保存は`workplace,workplace_question`、private、Git候補falseを強制する

### 7.4 現場適応・進捗報告文

- `buildReportText`等で生成した完成報告文を共有確認画面へ渡す
- プロンプトは追加せず、完成報告文だけを既定共有内容とする
- 全文編集可能
- 現場適応用守秘チェックを必須にする
- 保存する場合は既存notesを利用し、次を強制する
  - `type`: `thought`
  - `tags`: `workplace,workplace_report`
  - `visibility`: `private`
  - `is_git_candidate`: `false`
  - 一般的な日付入りタイトル

## 8. 共有確認画面

### 8.1 表示項目

- 画面タイトル：`外部で使う内容を確認`
- 出所
  - プロンプト名
  - さくっとメモ
  - 現場適応・質問
  - 現場適応・報告
- 用途選択（さくっとメモのみ）
- 編集可能な全文入力欄
- 通常注意または現場適応用守秘チェック
- コピー
- ChatGPTなどへ共有
- 新しい記録として保存
- キャンセル／戻る

### 8.2 全文入力欄

- 長文を内部スクロールできる
- 改行、日本語、記号を維持する
- 現在の編集位置がキーボードに隠れない
- 共有前に全文を確認できる
- 空文字の場合はコピー・共有・保存を無効にする

入力挙動は`31`の長文入力基準と整合させる。

### 8.3 通常注意

プロンプト集・さくっとメモでは、次の趣旨を表示する。

> 氏名、会社名、認証情報、非公開URLなど、外部サービスへ渡してはいけない情報が含まれていないか確認してください。

毎回のチェックボックスは必須にしない。

### 8.4 現場適応用守秘チェック

質問・報告では次の3項目を必須にする。

- [ ] 顧客名・会社名・個人名を含んでいない
- [ ] 認証情報・社内URL・内部システム名を含んでいない
- [ ] 会社固有の手順・判断基準・非公開情報を含んでいない

3項目すべてが確認されるまで`ChatGPTなどへ共有`を無効にする。

コピーについても外部持ち出しにつながるため、現場適応では同じチェック完了後に利用可能とする。

#### 8.4.1 編集後の再確認（2026-08-02 追加。安全補強）

守秘チェックは**実際に外部へ出す最終文章**に対する確認であるため、3項目を確認した後に共有確認画面で本文を編集した場合は、確認済み状態を破棄し、コピー・共有の前に3項目を再確認させる。

* 対象は現場適応系（質問・報告）のみ。プロンプト集・さくっとメモ（通常系）は対象外
* 本文が実際に変わった場合だけ解除する（同じ内容の再通知では解除しない）
* 解除後はコピー・共有を無効に戻し、3項目を再確認すると再び利用できる
* **保存**（現場適応専用保存＝private・Git候補false）は守秘チェックと連動させない（§20.5の適用範囲を拡大しない）

これは新機能・新Phaseではなく、既存の守秘チェック（`23` §5.1）が最終文章へ適用されるための安全補強である。

## 9. コピー

### SHARE-COPY-01 維持

既存コピー機能を削除・共有へ置換しない。

### SHARE-COPY-02 結果表示

- 成功：`コピーしました`
- 失敗：`コピーできませんでした`
- コピー中は二重実行を防止する
- 結果表示は一定時間後に通常状態へ戻す

### SHARE-COPY-03 コピー内容

画面に表示されている編集後全文と、クリップボード内容が一致すること。

## 10. OS・ブラウザ共有

### SHARE-OS-01 Android

- React Native／Expoで利用可能な標準共有機能を第一候補とする
- 新規依存を追加せず実現できる方法を優先する
- OSの共有先一覧を開く
- ユーザーがChatGPT等を選ぶ

### SHARE-OS-02 Web

- ブラウザ共有機能が利用可能な場合は共有画面を開く
- 利用できない場合はエラーで終了せず、次を案内する

> この環境では共有画面を開けないため、コピーして利用してください。

- コピーは引き続き利用可能にする

### SHARE-OS-03 iOS

- Phase 16Cの必須完了条件には含めない
- 標準共有機能で明らかな回帰を持ち込まない

### SHARE-OS-04 完了表示

MindHubが表示してよい表現：

- `共有画面を開きました`
- `共有を開始しました`
- `共有画面を開けませんでした`

表示してはいけない表現：

- `ChatGPTへ送信しました`
- `共有が完了しました`
- `相手に届きました`

OSやブラウザから明確なキャンセル結果を取得できない場合は、推測で成功扱いにしない。

## 11. 新しい記録として保存

### SHARE-SAVE-01 基本

- 共有用編集文章は一時データ
- 保存を選択した場合だけ、新しい`notes`レコードとして保存する
- 元のプロンプト、さくっとメモ、質問・報告の元データを上書きしない

### SHARE-SAVE-02 通常系

プロンプト集・さくっとメモからの保存は、既存の記録作成画面へ本文を引き継ぎ、ユーザーがタイトル・カテゴリ・タグ等を確認して保存する。

- 自動保存しない
- Git候補・visibilityは既存記録作成画面のルールに従う
- 引き継ぎ方法は実装判断とする

### SHARE-SAVE-03 現場適応系

質問・報告は、一般の記録作成画面へ渡して守秘既定を変更できる状態にせず、現場適応専用保存処理でprivate・Git候補falseを強制する。

### SHARE-SAVE-04 保存タイミング

`新しい記録として保存`は共有確認画面から利用できる。コピー・共有の前後を問わないが、保存は必須ではない。

## 12. 一時データ

- 共有確認画面を閉じ、保存していない編集文章は破棄してよい
- 共有履歴をDBへ残さない
- 共有先アプリ名を保存しない
- 共有日時を元メモへ記録しない
- 共有後にMindHubへ戻った場合、画面が生存している間は編集文章を維持してよい

## 13. ルート・画面構成

共有確認画面を共通ルートとして追加できる。

具体的なルート名は実装判断とするが、次を満たすこと。

- プロンプト集、さくっとメモ、質問、報告から再利用可能
- Web直アクセス時に空白・クラッシュ・機密データ表示を起こさない
- 共有対象をURLクエリへ全文で入れない
- 戻るフォールバックがある
- 一時文章を不用意にログへ出さない

## 14. データ・セキュリティ

- 共有本文をSQLiteへ自動保存しない
- URL、ログ、エラー、analyticsへ共有本文を出さない
- 認証情報を検出・送信する機能を作らない
- 外部アプリへ渡した後のデータ管理は共有先サービスの範囲であることを注意する
- 現場適応の守秘ルールは一般共有ルールより優先する
- GitHubアップロード、公開HTML、配布用出力とは別機能として扱う

## 15. エラー・例外処理

- 共有本文が空：コピー・共有・保存を無効化し理由表示
- 共有API利用不可：コピー案内
- 共有画面起動失敗：編集文章を維持し失敗表示
- 共有キャンセル：編集文章を維持し、エラー・送信済みにしない
- 保存失敗：編集文章を維持し再保存可能
- コピー失敗：共有は利用可能なまま
- 共有中の連打：多重起動防止
- 画面アンマウント後の状態更新・タイマー残留を防止

## 16. アクセシビリティ

- コピー、共有、保存、キャンセルは44相当
- 処理中、無効、結果を文字と`accessibilityState`で示す
- 守秘チェックの選択状態を読み上げ可能にする
- 共有確認全文をスクリーンリーダーで確認できる
- 最大フォントサイズでも共有・戻るへ到達できる

## 17. 受入条件

判定状況（2026-08-08）：**Gate A〜G すべてPASS**。Gate A・B・C・E・F・GはWeb確認（2026-08-02）で、Gate DはEAS APK versionCode 10 のPixel最終受入（2026-08-08。§23.7）で確認した。

### 17.1 Gate A：プロンプト集

- [x] 既存コピーが機能する
- [x] 各プロンプトから共有確認へ進める
- [x] 選択したプロンプト本文が欠けない
- [x] 検索・絞り込み・展開・42件を壊していない

### 17.2 Gate B：さくっとメモ

- [x] 5用途を選べる
- [x] 既定は`内容を整理する`
- [x] プロンプト＋本文を1つの文章として編集できる
- [x] 編集しても元メモが変わらない
- [x] 本文コピー、編集、削除、GitHub連携を壊していない

### 17.3 Gate C：現場適応

- [x] 質問文・報告文を共有確認へ渡せる
- [x] 3つの守秘チェック完了前はコピー・共有できない
- [x] 保存時にprivate・Git候補falseが強制される
- [x] 会社固有情報の入力を促す文言がない

### 17.4 Gate D：Android共有

- [x] OS共有画面が開く
- [x] ChatGPTが候補にある場合、ユーザーが選択できる
- [x] 特定の共有先アプリのインストール有無に依存せず、Android標準のOS共有画面を利用する
- [x] 共有キャンセル後にMindHubへ戻れる
- [x] 長文・日本語・改行が欠けない
- [x] 多重共有画面が開かない

3項目目は、2026-08-08に**受入条件を実装アーキテクチャへ合わせて整理**したものである（旧条件：「ChatGPT未インストールでもクラッシュしない」）。MindHubは `src/utils/share.ts` でAndroid標準の `Share.share({ message })` を呼ぶだけで、**ChatGPTのpackageを指定せず・ChatGPTアプリを直接起動せず・インストール有無も検査しない**。未インストールのアプリはOS共有候補に表示されないだけで、共有先候補の列挙はAndroidへ委ねている。したがって特定アプリの有無を実機で切り替える確認ではなく、**標準OS共有を利用していること**を受入条件とする。**ChatGPTをアンインストールしての実機確認は実施していない**（経緯は §23.7）。

### 17.5 Gate E：Web

- [x] 共有対応ブラウザでは共有を開始できる
- [x] 非対応ブラウザではコピー案内を表示する
- [x] 360 / 390 / 412 / 480 / 768 / 1024幅で操作できる
- [x] Web直アクセスで本文漏えい・空白画面・クラッシュがない

### 17.6 Gate F：保存

- [x] 通常系は新しい記録作成へ引き継げる
- [x] 現場適応系は専用安全既定で保存される
- [x] 元データを上書きしない
- [x] 保存しなければDBへ残らない
- [x] 保存失敗後に再試行できる

### 17.7 Gate G：表示文言

- [x] `ChatGPTへ送信しました`等の誤表示がない
- [x] コピー、共有開始、保存を区別して表示する
- [x] 共有先をMindHubが保証しないことが分かる

## 18. Claude Codeへ任せてよい判断

- React Native標準`Share`、Web Share API等の具体的利用方法
- 共通共有画面のroute名・component名・hook名
- 一時文章の安全な画面間引き継ぎ方法
- 既存プロンプトIDと5用途の固定マッピング
- Web共有可否の検出方法
- 共有起動中の状態管理
- 保存画面へのprefill方法
- 確認用テスト・ハーネス

ただし、既存プロンプトの意味を確認し、用途と合わないマッピングを行わないこと。判断理由と採用マッピングを実装報告に記載する。

任せない判断：

- コピー機能の削除
- ボタン名の変更
- API・Webhook・新規認証の追加
- 自動送信
- 共有履歴保存
- 現場適応の守秘チェック省略
- 元データ上書き
- 共有完了の断定
- 新規依存追加

## 19. 実装単位

### 16C-1 共通共有基盤

- 共有確認画面
- 編集・コピー・OS共有
- Web fallback
- 二重操作・エラー状態

### 16C-2 プロンプト集・さくっとメモ

- 共有入口
- 5用途
- プロンプト固定マッピング
- 通常注意
- 新規記録作成への引き継ぎ

### 16C-3 現場適応

- 現場適応の質問・報告の**共有接続**（16C-1の共通共有確認画面へ渡す）
- 必須守秘チェック（3項目完了前はコピー・共有できない。`23` §5.1）
- **報告文の任意保存**（現場適応専用保存処理で private・Git候補false 強制。`workplace,workplace_report`）
- Android実機・回帰確認

※**完成質問文の任意保存そのものは 16B-3**（`32` §20）が担当し、16C-3では質問・報告を共有確認画面へ接続する。同じ保存処理を16B-3と16C-3で二重実装しない（現場適応専用保存処理を共用する）。

## 20. 実装記録（2026-08-02）

16C-1／16C-2／16C-3を1バッチで実装した。**この時点では** §1〜§19 に変更を加えていない（その後、再レビュー対応で §8.4.1 の安全補強を追加した。§22）。

### 20.1 実装場所

| 役割 | ファイル |
|---|---|
| 共有確認画面（route `/share/confirm`） | `app/share/confirm.tsx`（新規） |
| 画面間の一時引き継ぎ（共有対象／記録作成prefill） | `src/features/share/shareHandoff.ts`（新規） |
| 5用途・プロンプト固定マッピング・共有文章の組み立て | `src/features/share/shareTargets.ts`（新規） |
| OS／ブラウザ共有の起動 | `src/utils/share.ts`（新規） |
| ルート登録（title・戻るフォールバック） | `app/_layout.tsx` |
| プロンプト集の共有導線 | `app/prompts/index.tsx` |
| さくっとメモ詳細の共有導線 | `app/memo/[id]/index.tsx` |
| 現場適応 質問・報告の共有導線 | `app/workplace/question.tsx`・`app/workplace/report.tsx` |
| 報告文の現場適応専用保存（`saveReportNote`） | `src/features/workplace/workplaceService.ts` |
| 記録作成へのprefill受け取り | `app/notes/create.tsx` |
| 保存ヒント文の上書き許可（onSaveなしの場合） | `src/components/WorkplaceSceneForm.tsx` |

DB・`schema.ts`・`migrations.ts`・新規npm依存・`app.json`・`eas.json`・versionCodeは変更していない。共有はReact Native標準 `Share`（native）とWeb Share API（web）だけで実現している。

### 20.2 5用途の固定マッピング（§7.2）

| 用途 | 依頼文の取得元 | 採用理由 |
|---|---|---|
| 内容を整理する | 既存プロンプト `thought`（`chatgptPrompts.ts` の思考メモ整理） | 「ダーッと書く／整理すると／決める必要があること」等の見出しを持ち、未整理のメモ本文を整理する目的に一致する |
| やることを抽出する | 既存プロンプト `brain_dump_to_action` | 「今日やる／後日やる／保留／やらなくていい／確認が必要」に分ける定義で、やること抽出に一致する |
| 質問内容を整理する | **共有専用の固定依頼文**（§20.3の例外） | 既存42件に質問内容を整理する意味の定義がないため |
| 開発依頼に整える | 既存プロンプト `claude_prompt`（Claude Code用プロンプトとして整理） | 作業目的・変更範囲・完了条件・やらないことを揃える定義で、開発依頼に整える目的に一致する |
| 自由に編集する | 依頼文なし（さくっとメモ本文のみ） | §7.2の定義どおり |

制約：

* 用途1・2・4は `getPromptGroups()` からIDで取得する。プロンプト本文を別の定数へ複製しない（42件との二重管理を作らない）
* マッピングはIDで固定し、名称の部分一致で自動選択しない
* 共有文章の構造は `【依頼内容】` ＋ `【整理対象】` で全用途共通（用途5は本文のみ）

### 20.3 用途3の例外（共有専用の固定依頼文）

* 用途1・2・4は**既存プロンプト定義を再利用**する
* 用途3は**既存42件に意味の合う定義がない**ため、共有文章の組み立て専用の**固定依頼文**を使う（実装：`src/features/share/shareTargets.ts` の `QUESTION_ORGANIZE_REQUEST`）
* この固定依頼文は次のいずれにも含めない：プロンプト集の42件／`MOBILE_PROMPTS`／`NOTE_CATEGORIES`／Prompt Hubの検索・分類／DBテンプレート（`note_templates`）／ユーザー編集可能なプロンプト／`35` の用途別プロンプト編集構想
* **意味の合わない既存プロンプトを流用しないための例外**であり、用途3を減らす・保留するための措置ではない
* 現場適応の質問構造（`21`〜`23`・`32`）を参考にした共有専用仕様であり、`buildQuestionText`（未記入欄を含む完成質問文の生成）は呼び出さない
* 新しいPhase番号は作らない

### 20.4 既存プロンプト末尾の入力待ち行

* 既存プロンプト定義・42件の内容・生成HTML（`scripts/generate_prompt_hub.mjs` の出力）は変更しない
* 共有文章の組み立て時のみ、本文末尾に**完全一致**する入力待ち行（`整理対象：` / `整理対象（説明文）：`）を表示用に取り除く
* 本文途中に現れる「整理対象」の記述は削除しない
* 除去後に共通形式の `【整理対象】` 見出しとさくっとメモ本文を付ける

### 20.5 守秘チェックの適用範囲（判断B・2026-08-02決定）

* 守秘3チェックを必須にするのは、**新設した共有確認画面内の「コピー」と「ChatGPTなどへ共有」**（現場適応系のみ）
* `/workplace/question`・`/workplace/report` の**既存出力コピーは現状のまま維持**し、守秘チェックを追加しない
* 理由：§8.4は共有確認画面の仕様として定義されている／既存コピー機能を壊さない（§3・§9 SHARE-COPY-01）／Phase 16Cでは新しい外部受け渡し導線に確認手順を追加する／既存画面には既に守秘注意文（`WORKPLACE_PRIVACY_NOTICE`）が常時表示されている
* `23` §5.1 の「コピーも無効にする」は、この共有確認画面内のコピーを指すものとして実装した

### 20.6 一時データの扱い（§12・§13）

* 共有対象・共有用編集文章・記録作成prefillは、いずれもモジュール変数のみで保持する（URLクエリ・route params・SQLite・ログ・エラー・analyticsへ出さない）
* 受け側は `useState` 初期化関数で **get（純粋な読み取り）**してstateへ取り込んだ後、初回commit後の `useEffect` で **clear** する（Strict Mode安全。stateへ取り込む前には消さない）
* Webのリロード後・直アクセスでは復元されない（共有確認画面は「共有する内容がありません」＋ホームへ戻るを表示する）
* OS共有画面から戻っただけでは編集中の文章を消さない（共有・コピー・保存はいずれも画面stateを保持する）
* 共有確認用（`SharePayload`）と記録作成prefill用（`NoteDraftHandoff`）は別スロット・別型・別関数名に分離し、混線しない

### 20.7 共有結果の表示（§10 SHARE-OS-04）

* 表示するのは `共有を開始しました`（処理中）／`共有画面を開きました`／`共有画面を開けませんでした`／`この環境では共有画面を開けないため、コピーして利用してください。` のみ
* Web Share APIの `AbortError`（キャンセル）・iOSの `dismissedAction` は、失敗表示にも送信済み表示にもしない（編集文章を維持したまま通常状態へ戻す）
* `共有が完了しました`／`ChatGPTへ送信しました`／`相手に届きました` は表示しない

### 20.8 検証状態（2026-08-02時点）

実施済み。

* `npx tsc --noEmit` 合格
* `npx expo export --platform web` 成功（バンドルエラー0。共有ディレクトリ `dist` が別プロセスに使用中だったため、出力先をリポジトリ外へ指定して実行）
* `npx expo-doctor` 17/18（失敗1件は既存の `expo 54.0.35` vs `~54.0.36` パッチ差＝本実装と無関係・未修正）
* `git diff --check`：**初回実装コミット（`5e76a29`）では末尾空白の通知1件**（本ファイル冒頭のMarkdownハード改行2スペース）。レビュー修正コミットで解消し、`git diff --check d1a0600..HEAD` は合格（§21.3）
* 用途マッピング・末尾行除去の単体確認（実モジュールをコンパイルしてNode実行）＝**32/32合格**（42件維持・ID重複なし・用途1/2/4が既存本文と一致・末尾行のみ除去・本文途中の記述は残る・用途3が固定依頼文で42件に含まれない・用途5は本文のみ・組み立て構造）
* Web確認（ヘッドレスChrome・専用ポート8117・使い捨てuser-data-dir・puppeteerはリポジトリ外scratchpad隔離）＝**82項目中80合格**。5用途と既定・共有文章の構造・元メモ非変更・空文字時の無効化と理由表示・共有結果4分岐（成功／キャンセル／失敗／非対応）・禁止文言なし・幅6種（360/390/412/480/768/1024）で横スクロールなし・記録作成prefillとリロード非復元・共有確認の直アクセス空状態と戻る・プロンプト集42件維持と共有導線・現場適応の守秘3チェック（未完了ではコピー／共有不可、3件完了で可）・報告文の専用保存（`/notes` 詳細で private・`workplace_report`・Git候補外）・質問画面の既存コピー／保存／完了導線の維持・console.error／pageerror／unhandledrejection 0件

未確認・未実施。

* **コピー成功経路**（ヘッドレスChromeが `clipboard-write` を拒否＝環境制約。失敗表示「コピーできませんでした」と一定時間後の通常状態復帰は確認済み。Phase 15と同じ既知事象）
* **Android実機確認（Expo Go）**：未実施
* **EAS APK更新**：未実施。新規ネイティブ依存はないため実装commit前のビルドは不要だが、**Phase 16Cのコード変更を配布APKへ反映するにはCodexレビュー合格後にPhase 16全体を含むEAS APK更新が必要**
* **更新APKでのPixel最終確認**：未実施。Expo Go確認は事前確認であり、最終確認は更新APKで行う
* `33` Gate A〜G のうち、Gate D（Android共有）は未確認。`32` Gate D（共有・守秘）もAPK確認後に判定する
* iOSは Phase 16Cの必須完了条件外（§10 SHARE-OS-03）

## 21. レビュー対応記録（Codex初回レビュー・2026-08-02）

対象コミット：`5e76a291a0eebad57b5e0285102ab3cedad7598b`（初回実装）。
初回レビュー結果：Critical 0／High 0／**Medium 4**／**Low 4**／Note 4／判定＝要修正・再レビュー。
対応：8件すべてを修正（初回コミットはamendせず、追加コミットで対応）。**この時点でも** 仕様（§1〜§19）の製品判断は変更していない（§8.4.1 の追加は後続の再レビュー対応。§22）。

### 21.1 Medium 4件

| # | 指摘 | 対応 |
|---|---|---|
| M1 | 用途選択で編集内容を確認なしに失う | 用途ごとに「生成直後の文章（baseline）」を保持し、`text !== baseline` で編集済みを判定。**同じ用途の再選択はno-op**、未編集なら即時変更、編集済みは既存の `confirmDialog`（Web＝`window.confirm`／Android＝`Alert.alert`）で「用途を変更／現在の編集内容は破棄されます」を確認。キャンセル時は用途・文章・各種結果表示をいずれも変更しない |
| M2 | 通常系保存の高速二重押下で `/notes/create` が複数pushされる | 共通hook `src/hooks/useNavigationLock.ts` を新設し、**同期refでロック**。順序は「ロック → `setNoteDraftHandoff` → `router.push`」。`router.push` が同期例外なら `clearNoteDraftHandoff()` してロック解除（本文はログ・表示へ出さない） |
| M3 | 4つの共有入口で遷移の二重操作防止がない | プロンプト集・さくっとメモ詳細・現場適応の質問／報告の4入口にも同じhookを適用。遷移中はボタンを無効化し `accessibilityState.disabled` / `busy` を設定。遷移が同期失敗した場合は `clearSharePayload()` |
| M4 | 現場適応保存後に文章を編集しても保存済み表示とdisabledが残る | 保存中は全文入力欄を `editable=false`（`accessibilityState.disabled`）にし、**保存開始時点の文章を保存処理へ渡す**（競合防止）。文章変更時は保存タイマーを解除して `idle` へ戻し、再保存できるようにした |

### 21.2 Low 4件

| # | 指摘 | 対応 |
|---|---|---|
| L5 | 文章変更後も以前のコピー成功表示が残る | `useCopyFeedback` に `reset()` と**世代（generation）管理**を追加。文章変更・用途変更確定時に `reset()` を呼ぶ。コピー処理中に文章が変わった場合、進行中の処理は中断しないが、完了時に世代が異なれば結果を表示せず `idle` へ戻す。処理中の二重コピー防止は維持。既存利用箇所は従来どおり（後方互換） |
| L6 | プロンプトID取得失敗時に無言で自由編集相当へ変わる | `shareTargets.ts` を結果型へ変更（`ShareRequestResult` / `ShareTextResult`＝`ok: true` と `ok: false, message` の判別可能ユニオン）。**用途5の「依頼文なし」と取得失敗を型で区別**。取得失敗時は本文だけへフォールバックせず、`選択した用途の文章を準備できませんでした。別の用途を選んでください。` を表示し、コピー・共有・保存を無効化。内部ID・本文はメッセージへ含めない。別用途を選べば復旧できる |
| L7 | `git diff --check` が非ゼロなのに文書では問題なしとしている | 本ファイル冒頭の変更行から末尾2スペースを外し、通常の空行で表示を維持。§20.8の記述を実際の結果（初回コミットで通知1件／レビュー修正で解消）へ訂正。`current-tasks.md`・`docs/worklog/current.md` も同様に訂正 |
| L8 | `workplaceTags.ts` の報告保存コメントが実装状態と一致しない | 「Phase 16C-3で追加予定」→「Phase 16C-3で実装済み」へ修正（定数値・タグ値は無変更） |

### 21.3 レビュー修正後の検証

* `npx tsc --noEmit` 合格
* `npx expo export --platform web` 成功（`dist` が別プロセス使用中のため出力先はリポジトリ外）
* `npx expo-doctor` 17/18（失敗1件は既存の `expo` パッチ差＝無関係）
* `git diff --check` 合格（作業ツリー・`d1a0600..HEAD` とも exit 0）
* 用途マッピング・末尾行除去・**ID取得失敗の結果型**の単体確認＝**43/43合格**（42件維持・ID重複なし・用途5の「依頼文なし」と取得失敗の区別・失敗メッセージに内部IDを含まない）
* Web確認（レビュー修正分。ヘッドレスChrome・専用ポート8117）＝**41/41合格**（同一用途の再タップno-op／未編集の即時変更／編集済みの確認表示・キャンセルで維持・確定で再生成／5経路の高速二重押下でpush1回・戻ると再操作可／保存中は入力不可・保存後の編集でidle復帰・再保存で新規レコード／文章変更でコピー・共有結果を解除・用途変更キャンセルでは解除しない）
* 初回実装分のWeb確認を回帰実行＝**83項目中81合格**（不合格2件はコピー成功経路＝ヘッドレスの `clipboard-write` 拒否という既知の環境制約）
* コピー成功経路は引き続き未確認（通常ブラウザ・実機で確認する）。ID取得失敗時の画面表示は、正常なマッピングでは発生しないため単体確認とコード上の無効化条件で担保している

### 21.4 レビュー対応後も残る状態

Codex**再レビュー待ち**（→ §22で実施）／EAS APK更新待ち（Codexレビュー合格後にPhase 16全体を含む更新が必要）／Expo Go事前確認待ち／更新APKでのPixel最終確認待ち。**Phase 16Cは最終完了としない**。

## 22. レビュー対応記録（Codex再レビュー・2026-08-02）

対象コミット：`c541ab12bf702c3c6c795a69b44124090f9383f7`（初回レビュー対応）。
再レビュー結果：Critical 0／**High 1**／Medium 0／**Low 1**／Note 4／判定＝要修正・再レビュー。
対応：2件とも修正（既存コミットはamendせず追加コミット）。§1〜§19の製品判断は変更せず、§8.4.1として既存守秘チェックの安全補強を明文化した。

### 22.1 H1：本文編集後も守秘チェックが維持される

* 症状：現場適応（質問・報告）で守秘3項目を確認した後に共有本文を編集すると、確認済み状態が残り、**編集後の最終文章を再確認しないままコピー・共有できた**
* 修正：`app/share/confirm.tsx` の本文変更処理で、**本文が実際に変わった場合のみ**（`next === text` は何もしない）、現場適応系に限り守秘3チェックをすべて未選択へ戻す。すでに全項目falseなら状態更新しない
* 影響範囲：`workplace_question`・`workplace_report` のみ。`prompt`・`memo` は対象外
* 解除後はコピー・共有が `disabled`（`accessibilityState.disabled`）へ戻り、既存の必須確認文「3項目すべてを確認するまで、コピーと共有はできません。」を再表示する。3項目を再確認すれば再び利用できる
* **保存は守秘チェックと連動させない**（現場適応の保存はprivate・Git候補falseを強制する内部保存であり、§20.5の適用範囲を拡大しない）
* 既存挙動（`copy.reset()`・共有結果の解除・保存状態の解除・保存後の再編集・保存中の編集不可・用途変更・通常系の操作）は維持

### 22.2 L1：Gate・現在状態の文書不一致

* `10-tasks.md` 16C-3のAndroid実機確認欄から **Gate E（Web確認・確認済み）を除外**し、Gate D（Android共有）のみ未確認とした
* 現在状態を案内する文書の古い表現を更新：`00_START_HERE.md`・`CLAUDE.md`・`AGENTS.md`（Phase 16案内行）・`docs/flowdock_mvp_design.md` §4.4.1・`01-decisions-and-scope.md` §1.8・`35` §1.2・`08`/`14`/`16`/`21`/`29` の Phase 16 追記見出し（「未着手」→状態の正本参照へ）
* 過去時点の作業履歴・レビュー記録（`docs/worklog/current.md` の各回記録、`current-tasks.md` の過去エントリ、`31` §16の凡例など）は**歴史記録として変更しない**

### 22.3 Gateの現在状態

| Gate | 状態 |
|---|---|
| `33` Gate A（プロンプト集）・B（さくっとメモ）・C（現場適応）・F（保存）・G（表示文言） | Web範囲で確認済み |
| `33` Gate E（Web） | 確認済み |
| `33` Gate D（Android OS共有・実機） | **未確認** |
| `32` Gate D（現場適応の共有・守秘） | **更新APKでのAndroid確認後に判定** |

Expo Go事前確認・EAS APK更新・更新APKでのPixel最終確認はいずれも**未実施**。

### 22.4 再レビュー修正後の検証

* `npx tsc --noEmit` 合格／`npx expo export --platform web` 成功（`dist` 使用中のため出力先はリポジトリ外）／`npx expo-doctor` 17/18（既存の `expo` パッチ差のみ）／`git diff --check` 合格（作業ツリー・`d1a0600..HEAD` とも exit 0）
* 単体確認＝**43/43合格**（42件維持・ID重複なし・5用途・ID取得失敗の結果型）
* Web確認（H1のパターンA〜F＋回帰）＝**48/48合格**。質問・報告の両方で「3項目選択→有効→本文編集→全解除→コピー／共有無効→再選択で有効」、部分選択でも編集で解除、全未選択のまま編集してもエラーなし、同じ文字列の再通知では解除しない、編集でコピー結果・共有結果・保存状態も解除、保存後の編集で再保存可・再共有には再確認が必要、通常系は影響なし
* 既存Web確認の回帰＝初回レビュー修正分 **41/41合格**、初回実装分 **83項目中81合格**（不合格2件はコピー成功経路のみ）
* コピー成功経路は引き続きヘッドレス環境制約で未確認（`clipboard-write` 拒否）
* 付随修正：react-native-webが `accessibilityState.checked` を `aria-checked` へ反映しないことを実測で確認し、チェックボックスへ `aria-checked` を明示追加した

### 22.5 状態

Codex**再々レビュー待ち**（→ §23で実施）／EAS APK更新待ち／Expo Go事前確認待ち／更新APKでのPixel最終確認待ち。**Phase 16Cは最終完了としない**。

## 23. レビュー結果記録（Codex再々レビュー・2026-08-02）

対象HEAD：`32303cb4fb4626d61c4269f8ab23adc06f343f34`（再レビュー対応）。
再々レビュー結果：Critical 0／High 0／Medium 0／**Low 1**／Note 4／判定＝要修正・再レビュー。

### 23.1 判定内容

* **H1は解消**（守秘3チェックが編集後の最終文章へ適用されることを確認）
* 初回レビュー8件に**回帰なし**
* `aria-checked` の追加に**問題なし**
* **製品コード上の追加指摘なし**
* 残件は**文書の現在状態不一致（Low 1件）のみ**

### 23.2 今回（文書Low 1件）の修正

* `current-tasks.md`：先頭の現在フェーズを本コミット後の状態へ更新。名称整理エントリ本文の「main反映待ち」「Phase 16C未開始」を、PR #1 merge済み・main反映済み・ランチャー表示名は次回の更新APKで最終確認、へ訂正
* `10-tasks.md` §18.1：同じ本文の古い状態を同様に訂正（見出しは既にPR #1 merge済み・main反映済み）
* 本ファイル冒頭の実装状態：「Codex再レビュー待ち」→ 再々レビューの結果と**Codex最終文書確認待ち**へ更新。参照先を §20〜§23 に整合
* 「§1〜§19の仕様内容は変更していない」→ 当初の製品判断・責務分担は維持しつつ、再レビュー対応で §8.4.1 の安全補強を追加した旨へ修正。§20・§21 の同種の記述は**その時点の記録**であることを明示
* `docs/worklog/current.md`：本回の記録を追加（過去回の記録は時系列として維持）

コード・DB・schema・migration・依存・`app.json`・`eas.json`・versionCode は変更していない（**コード変更なしのため、既存の検証結果を維持**）。

### 23.3 状態

Codex**最終文書確認待ち**／Expo Go事前確認待ち／EAS APK更新待ち／更新APKでのPixel最終確認待ち／`33` Gate D・`32` Gate D 未通過／PR作成・mainへのmerge未実施。**Phase 16Cは最終完了としない**。

※上記は**この時点（`78625c4` コミット時）の状態**である。その後に実施したCodex最終文書確認の結果とその修正は §23.4 を参照。**現在状態は本文書冒頭の「実装状態」を参照**する。

### 23.4 Codex最終文書確認とLow 2件の修正（2026-08-05）

対象HEAD：`78625c444b0a1c45da88317eeb9f146167e9fe5e`。
最終文書確認結果：Critical 0／High 0／Medium 0／**Low 2**／Note 5／判定＝要修正・再レビュー。

#### 確認済み（指摘なし）

* `10-tasks.md` §21と16C-1〜3のレビュー段階は、その後（`c1828b5`）修正済み。`[ ]`・Gate D未通過・Android未確認の維持も適切
* 名称整理の本文（PR #1 merge済み・main反映済み）は修正済み
* 本文書の §8.4.1 と §23 は整合
* H1（守秘チェックの編集後再確認）は解消済み
* **製品コード上の新規問題なし**

#### Low 2件と修正

1. **`current-tasks.md` で `78625c4` と `c1828b5` のtsc実行時系列が不明確** → 先頭項目に検証実績を**コミット単位**で明記した（`78625c4` の回は任意の `npx tsc --noEmit` と `git diff --check` が合格＋検索・参照確認。`c1828b5` の回はtscを再実行せず、`git diff --check`・文書検索・相互確認・見出し／内部参照確認のみ。`expo export`・`expo-doctor`・Web・単体は両回とも再実行せず既存結果を維持）
2. **本文書冒頭が「Codex最終文書確認待ち」のまま** → 冒頭の実装状態を2026-08-05時点へ更新し、最終文書確認の実施・Low 2件の修正済み・**Codex最終文書再確認待ち**へ整合させた

あわせて `docs/worklog/current.md` に今回の記録を追加した。`10-tasks.md` は今回のCodex確認で合格しているため変更していない。**コード・DB・schema・migration・依存・lockfile・`app.json`・`eas.json`・versionCode は変更なし**。

#### 状態（この時点）

Codex**最終文書再確認待ち**／Expo Go事前確認待ち／EAS APK更新待ち／更新APKでのPixel最終確認待ち／`33` Gate D・`32` Gate D 未通過／PR作成・mainへのmerge未実施。**Phase 16Cは最終完了としない**。

※上記は**この時点（`00dd897`・`c387df1` コミット時）の状態**である。その後に実施したCodex最終文書再確認の結果は §23.5 を参照し、**現在状態は本文書冒頭の「実装状態」を参照**する。

### 23.5 Codex最終文書再確認PASS（2026-08-05）

対象HEAD：`c387df13ac94193ed56a21e9bbede7db810770ff`。
再確認結果：Critical 0／High 0／Medium 0／**Low 0**／Note 0／**最終判定＝PASS**。

#### 確認された内容

* `current-tasks.md` 先頭の現在状態が「Codex最終文書再確認待ち」に一本化されていた
* 旧段階の「現在は Codex最終文書確認待ち」は解消済み
* 検証時系列がコミット単位（`78625c4`／`c1828b5`／`00dd897`・`c387df1`）で整合
* `docs/worklog/current.md` に自主確認による修正記録あり
* `current-tasks.md`・`10-tasks.md` §21・本文書冒頭と §23.3〜§23.4・worklog の現在状態が相互に整合
* コード・DB・依存・設定の変更なし

#### PASS後の状態（この時点）

* **製品コードと文書のCodexレビュー工程は完了**
* **PR作成・main反映へ進める**
* Expo Go事前確認は**未実施**
* EAS APK更新は**未実施**
* 更新APKでのPixel最終確認は**未実施**
* `33` Gate D・`32` Gate D は**未通過**
* **Phase 16Cは最終完了ではない**（Android受入後に最終完了を判定する）

※上記は**この時点（2026-08-05・Codex最終文書再確認時点）の状態**である。その後の経過は §23.6・§23.7、**現在状態は本文書冒頭の「実装状態」**を参照する。

### 23.6 Expo Go実機事前確認PASS（2026-08-05）

対象main：`b96cc669ddc7c33f3563070d85962a07bb3b85fd`（PR #3 merge commit）。
端末：**Pixel**／実行環境：**Expo Go・Expo SDK 54**（Metro：`exp://192.168.0.225:8118`）。
結果：**PASS**。

#### 合格した確認項目

* **A1〜A7（基本起動）**：Expo Goから起動・赤いエラー画面/クラッシュなし・ホーム表示・主要4カード表示と操作・Android戻る操作に問題なし・重大な表示崩れなし
* **P1〜P12（プロンプト集）**：一覧/展開/既存コピー・共有確認画面への遷移・本文表示/編集/コピー・OS共有起動・キャンセル後の復帰・復帰後も編集文章を維持・連打で多重起動しない
* **M1〜M16（さくっとメモ）**：作成/詳細・5用途表示・初期用途「内容を整理する」・用途変更・共有文章の編集・**元メモを変更しない**・コピー・OS共有・ChatGPTを共有先として選択・日本語/改行の保持・自動送信されない・戻った後も編集文章を維持・長文の先頭/中間/末尾を保持・多重共有なし
* **Q1〜Q16（現場適応・質問）**：共有確認画面への遷移・守秘3項目表示・完了前はコピー/共有無効・完了後は有効・OS共有起動・キャンセル後の復帰と質問文の維持・**本文変更後に3チェック全解除**・解除後は再び無効・再確認後は共有可能・誤った送信完了表示なし・保存は private/Git候補false・保存を守秘チェック必須へ拡張していない
* **R1〜R14（現場適応・報告）**：共有確認画面への遷移・守秘3項目表示・完了前は不可/完了後は可・本文編集後のチェック解除・OS共有起動・キャンセル後の復帰と報告文の維持・保存可能・private/Git候補false/専用タグ・二重保存なし・誤った共有完了表示なし
* **D1〜D10（Android共有横断／確認範囲）**：OS共有画面が開く・ChatGPTを共有先として選択可能・自動送信されない・キャンセル後にMindHubへ戻れる・日本語/改行/長文を保持・多重共有画面なし・共有後のクラッシュなし・実際の状態を超えた成功断定なし

不具合・クラッシュ・重大な表示崩れは検出されなかった。

#### 未確認

* **ChatGPT未インストール端末での実機確認**：確認のためのアンインストールは行っていない。Android標準のOS共有機能を使う実装であることは**コードレビュー済み**。この1項目はExpo Go事前確認のPASSを妨げないが、未確認であることは隠さない

#### PASS後の状態

* **Expo Go事前確認は完了**
* **EAS APK更新へ進める**（ユーザー承認後に実施）
* 更新APKで**同等項目を最終確認**する
* **§17.4 Gate D はまだ未通過**（`32` Gate D も同様）
* **Phase 16Cは最終完了ではない**

※上記は**この時点（2026-08-05・Expo Go事前確認時点）の状態**である。その後のEAS APK versionCode 10 による最終受入結果は §23.7 を参照し、**現在状態は本文書冒頭の「実装状態」を参照**する。

### 23.7 EAS APK versionCode 10 最終受入・Phase 16C完了（2026-08-08）

#### ビルド情報

| 項目 | 値 |
|---|---|
| Build ID | `10bedf3b-31c3-4fa7-aefe-73f62f960b5e` |
| Build URL | https://expo.dev/accounts/ykjob/projects/flowdock/builds/10bedf3b-31c3-4fa7-aefe-73f62f960b5e |
| APK | https://expo.dev/artifacts/eas/_fPMwwHwSgHXeHqTpmvN7iG5P6ZkeGKojNITU0WJ32o.apk |
| artifact有効期限 | 2026-08-19T06:54:09Z |
| status / platform | FINISHED / ANDROID |
| profile / distribution | preview / INTERNAL |
| appVersion / versionCode | 1.0.0 / **10** |
| SDK / applicationId | 54.0.0 / `com.ykjob.mindhub` |
| 対象commit | `8706d7d3fcbfa349a1b71c4d45bb65e7e61fbb0a`（`chore: bump android version code to 10`） |

EASビルドは**1回のみ**実行した。

#### Pixel最終受入（2026-08-08）：K1〜K22 すべてPASS

* **K1〜K5（起動・データ）**：Androidのアプリ名が「MindHub」／正常起動／**Expo Go・Metroなしの単体起動**／赤いエラー画面・クラッシュなし／**既存メモ・設定が保持**（versionCode 9 からの上書き更新に成功）
* **K6〜K10（OS共有）**：プロンプト集からOS共有画面が開く／さくっとメモからOS共有画面が開く／現場適応の質問・報告でも守秘3項目確認後に共有画面が開く／**共有先にChatGPTを選んでもMindHubから自動送信されない**／共有キャンセル・Android戻る操作後にMindHubへ戻り、**共有文章が維持される**
* **K11〜K16（内容保持・多重・表示）**：日本語の文字化けなし／改行保持／**長文の先頭・中間・末尾が欠けない**／共有連打でも共有画面が多重起動しない／キャンセル後もクラッシュなし／**実際に送信していないのに「送信完了」「共有完了」と誤表示しない**
* **K17〜K22（守秘チェック・コピー）**：守秘3項目未確認ではコピー・共有不可／3項目確認でコピー・共有可能／**確認後に本文を編集すると3チェックがすべて解除**／解除後は再び不可／再確認で再び可能／**K22：更新APK実機でコピー成功を確認**

**K22により、ヘッドレスWeb環境では環境制約（`clipboard-write` 拒否）で確認できなかった「コピー成功経路」も、実機で確認済みとなった**（`33` §20.8・§21.3・§22.4 に残る「コピー成功経路は未確認」は、それぞれの時点の記録）。

#### Gate Dの受入条件整理（ChatGPT未インストール条件）

* 旧条件：**「ChatGPT未インストールでもクラッシュしない」**
* 新条件：**「特定の共有先アプリのインストール有無に依存せず、Android標準のOS共有画面を利用する」**（§17.4）
* 理由：MindHubは `src/utils/share.ts` でAndroid標準の `Share.share({ message })` を呼ぶだけであり、**ChatGPTのpackage指定・直接起動・インストール有無の検査をいずれも行わない**。未インストールのアプリはOS共有候補に表示されないだけで、候補の列挙はAndroid側の責務である。旧条件は実装アーキテクチャに対して特定アプリ依存のテスト条件になっていたため、実装に合う条件へ整理した
* **ChatGPTをアンインストールしての実機確認は実施していない**（そのためのアンインストールを行っていない）。この事実は隠さず記録する。2026-08-05のExpo Go記録（§23.6）にある「ChatGPT未インストール端末は未確認」も歴史記録として残す

#### 判定

* **`33` §17 Gate A〜G：すべてPASS**
* **`32` Gate D（共有・守秘）：PASS**
* **Phase 16C：最終完了（2026-08-08）**

今回はクローズ作業のため、コード・DB・schema・migration・依存・`app.json`（versionCodeは10のまま）・`eas.json` は変更していない。EASの再ビルドも行っていない。利用者向けボタン名 `ChatGPTなどへ共有` は**v10の現行仕様として維持**し、次回versionCode 11のUXレビュー候補として `11-open-issues.md` に記録した（今回は採用確定ではない）。
