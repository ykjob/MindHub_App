# 現場適応モード 質問タイミング判断支援（Phase 16B）

追加日：2026-07-23  
文書状態：承認済み仕様／未実装  
対象Phase：Phase 16B  
対象リポジトリ：`ykjob/MindHub_App`

## 1. 文書の位置づけ

本ファイルは、現場適応モードに追加する「質問タイミング判断支援」の判定条件、結果、画面入力、引き継ぎ情報、受入条件の正本とする。

既存仕様との分担は以下とする。

| 内容 | 正本 |
|---|---|
| 現場適応モードの目的・スコープ | `20-workplace-adaptation-overview.md` |
| 5場面、画面遷移、質問文作成画面 | `21-workplace-adaptation-flows-and-ui.md` |
| 保存、既存notes統合、正式DB定義との関係 | `22-workplace-adaptation-data-and-integration.md`、`03-data-model.md` |
| 守秘、外部AI、公開・Git除外 | `23-workplace-adaptation-security-and-portfolio.md` |
| 質問タイミングの判定条件と結果 | 本ファイル |
| 外部アプリへのコピー・共有 | `33-external-handoff-and-sharing.md` |

本機能は職場の正式なルールや上司の指示を置き換えない。

## 2. 背景

現行の現場適応モードでは、詰まり記録で状況・試したこと・確認したいことを整理し、質問文作成で相手が答えやすい文章を生成できる。

一方、未経験者は次の判断に迷いやすい。

- まだ自分で調べるべきか
- 何分程度で質問へ切り替えるべきか
- 影響が大きい作業を自分の判断で進めてよいか
- 急ぎの確認と、通常の質問をどう分けるか
- 質問前に最低限何を確認すればよいか

本機能は、正解を自動決定するのではなく、質問タイミングを安全側に整理し、質問文作成へつなげるための補助機能とする。

## 3. 確定した製品判断

- 通常の調査目安は20分
- 完全に作業が止まっている場合の目安は10分
- 本番、顧客、個人情報、認証、権限、削除、セキュリティ等の危険条件では時間を待たず確認する
- 現場で正式な質問ルールが定められている場合は、そのルールを優先する
- 初期実装では10分・20分をユーザー設定できる画面を作らない
- 判定途中の入力と判定結果はDBへ保存しない
- 完成した質問文だけ、ユーザーが選んだ場合に保存できる
- 導線は「詰まり記録→質問タイミング確認→質問文作成」
- `判定せず質問文を作る`導線を用意し、判定を強制しない

## 4. 目的

- 質問を遅らせるべきでない条件を先に見つける
- 20分・10分の目安を、絶対ルールではなく安全な切り替え目安として使う
- 「なぜ今質問するのか」「なぜもう少し調べるのか」を表示する
- 質問時に必要な情報を、質問文作成画面へ引き継ぐ
- 質問をためらって抱え込むことを減らす
- 相手へ丸投げに見えにくい質問文を作る

## 5. 対象範囲

- 詰まり記録からの質問タイミング確認導線（現場適応入口から開く独立機能は追加しない）
- 質問タイミング確認画面
- 3段階の判定結果
- 質問文作成画面への一時引き継ぎ
- 完成質問文のコピー、外部共有、任意保存
- 一般化された守秘確認

## 6. 対象外

- AIによる自由文の自動リスク判定
- 会社固有のSLAや質問ルールの自動取得
- 質問先、回答期限、回答状況の監視
- Slackやメールへの自動送信
- タイマー通知、プッシュ通知
- メンバー・上司・チーム管理
- 判定履歴、質問履歴の専用テーブル
- 職場別の10分・20分設定画面
- 質問した回数や回答速度の評価
- 人事評価、勤務評価への利用

## 7. 基本原則

### 7.1 現場ルール優先

画面上に次の趣旨を表示する。

> 現場で質問・確認の時間や手順が決められている場合は、そのルールを優先してください。

ユーザーが「現場ルールあり」を選んだ場合、MindHub独自の10分・20分より現場ルールを優先する旨を結果へ表示する。現場ルール本文は保存しない。

### 7.2 危険条件優先

危険・高影響条件が1つでも該当する場合、経過時間や調査状況にかかわらず「すぐ確認する」を最優先する。

### 7.3 時間は目安

10分・20分は質問を禁止する時間ではない。短時間でも危険、権限外、期限影響がある場合は質問する。

### 7.4 ユーザーが最終判断する

結果は推奨であり、ユーザーは次のいずれも選べる。

- 質問文を作る
- もう少し調査する
- 判定を無視して質問文を作る
- 現場適応モードへ戻る

## 8. 画面フロー

```text
詰まり記録
  ↓
［質問タイミングを確認］
  ↓
質問タイミング確認
  ├─ すぐ確認する
  ├─ 早めに質問する
  └─ もう少し調査する
       ↓
［質問文を作る］または［調査へ戻る］
       ↓
質問文作成
       ↓
整理済み質問文
  ├─ コピー
  ├─ ChatGPTなどへ共有
  └─ 記録として保存（任意）
```

質問タイミング確認画面には、`判定せず質問文を作る`導線を置く。

## 9. 入力項目

入力負担を増やしすぎないため、初期実装では以下の6群に限定する。

### QT-INPUT-01 現場ルール

- 現場で質問タイミングや確認手順が決められている
  - はい
  - いいえ／分からない

「はい」の場合もルール本文は入力・保存させない。画面上で「現場ルールを優先」と案内する。

### QT-INPUT-02 危険・高影響条件

複数選択可。

- 本番環境・公開環境へ影響する
- 顧客・利用者へ影響する
- 個人情報・機密情報を扱う
- ID、パスワード、トークン、APIキー、権限を扱う
- データ削除、上書き、移行など元に戻しにくい操作
- セキュリティ、障害、事故につながる可能性
- 外部への送信、公開、リリースを伴う
- 自分の権限・担当範囲で判断してよいか分からない
- 該当なし

具体的な会社名、システム名、URL、認証情報は入力しない。

### QT-INPUT-03 作業停止状態

- 完全に止まっていて、次に安全に試せることがない
- 一部は進められる
- まだ調査を始めたばかり

### QT-INPUT-04 経過時間

- 10分未満
- 10分以上20分未満
- 20分以上

正確な時刻入力、タイマー、通知は初期実装に含めない。

### QT-INPUT-05 期限・周囲への影響

複数選択可。

- 今日中・直近の期限に影響する
- 他の人の作業を止める
- 手戻りが大きくなる可能性がある
- 仕様・要件の判断が必要
- 自分の裁量を超えている
- 該当なし

### QT-INPUT-06 最低限の確認

質問前に確認できる一般項目。

- 指示・チケット・仕様を読み直した
- マニュアル・README・過去記録を確認した
- エラー表示・ログ・再現条件を確認した
- 影響の小さい範囲で試せることを試した
- 何を聞きたいかを一文で言える

危険条件がある場合は、これらが未完了でも質問を遅らせない。

## 10. 判定結果

判定は次の3種類とする。

| 結果 | 意味 |
|---|---|
| すぐ確認する | 時間を待たず、作業を進める前に確認する |
| 早めに質問する | 現時点で質問へ切り替えることを推奨する |
| もう少し調査する | 影響の小さい範囲で確認を続けてよい |

### 10.1 優先順位

```text
すぐ確認する ＞ 早めに質問する ＞ もう少し調査する
```

複数条件が該当する場合は、最も緊急度の高い結果を採用する。

## 11. 判定ルール

### QT-RULE-01 すぐ確認する

以下のいずれかに該当した場合は「すぐ確認する」。

- QT-INPUT-02の危険・高影響条件が1つ以上
- 操作を続けると元へ戻せない可能性がある
- 権限・担当範囲を超える判断が必要
- 事故、漏えい、障害、顧客影響を防ぐため確認が必要

※「現場ルールで即時確認が求められている」かどうかは、QT-INPUT-01が「現場ルールあり／なし」しか取得せず、ルール本文や即時確認条件を取得しないため、MindHubは自動判定しない。現場ルールありの場合は QT-RULE-04 に従い「現場ルールを優先」と注意表示するに留め、ルールの具体的内容を推測しない（ルール本文の入力・保存も行わない）。

結果理由の例：

- 「本番・顧客への影響があるため、時間を待たず確認してください」
- 「元に戻しにくい操作のため、実行前の確認を優先してください」
- 「権限・担当範囲の判断が必要です」

### QT-RULE-02 早めに質問する

危険条件がなくても、以下のいずれかに該当した場合は「早めに質問する」。

- 完全に作業が止まった状態で10分以上経過
- 通常の調査を20分以上続けている
- 期限、他者の作業、手戻りへ影響する
- 仕様・要件の判断が必要
- 自分の裁量を超えている
- 最低限の確認を行ったが次の安全な調査手段がない

結果理由の例：

- 「完全に止まった状態が10分以上続いています」
- 「通常の調査目安20分を超えています」
- 「他の作業や期限への影響があるため、早めの共有が適切です」

### QT-RULE-03 もう少し調査する

次をすべて満たす場合に限り「もう少し調査する」。

- 危険・高影響条件がない
- 完全停止ではない、または完全停止10分未満
- 通常20分未満
- 期限・他者・手戻りへの大きな影響がない
- まだ確認していない一般的な資料または安全な試行がある

結果には、未確認項目から最大3件の次の行動を表示する。

例：

- 「指示・仕様をもう一度確認する」
- 「エラーと再現条件を整理する」
- 「あと10分調べる」のような強制カウントダウンは表示しない

### QT-RULE-04 現場ルールあり

- 現場ルールがある場合、結果の最上部に「現場ルールを優先」と表示する
- MindHubの判定が現場ルールと異なる可能性があることを明示する
- 現場ルール本文は記録しない

## 12. 結果画面

結果画面には以下を表示する。

1. 判定結果
2. 判定理由（最大3件）
3. 次に行うこと
4. 注意文
5. 操作ボタン

### 12.1 操作ボタン

結果が「すぐ確認する」「早めに質問する」の場合：

- `質問文を作る`（主要操作）
- `詰まり記録へ戻る`
- `現場適応へ戻る`

結果が「もう少し調査する」の場合：

- `調査内容を見直す`（詰まり記録へ戻る）
- `質問文を作る`
- `現場適応へ戻る`

判定結果によって質問文作成を禁止しない。

## 13. 質問文作成への引き継ぎ

判定画面の入力値と結果はDBへ保存せず、画面遷移中の一時情報として質問文作成へ引き継ぐ。

引き継ぎ候補：

- 判定結果
- 判定理由
- 急ぎ度の候補
- 詰まり記録の状況
- 試したこと
- 確認したいこと
- エラー内容

質問文作成画面では、引き継いだ内容を編集できること。

### 13.1 質問文への反映

- 「すぐ確認する」：急ぎ度へ「作業を進める前に確認したい」を候補表示
- 「早めに質問する」：急ぎ度へ「早めに確認したい」を候補表示
- 「もう少し調査する」から質問文作成へ進んだ場合：急ぎ度を自動決定しない

判定理由をそのまま相手へ送る必要はない。質問文へ含めるかはユーザーが編集する。

## 14. 完成質問文の保存

### QT-SAVE-01 保存対象

- 判定途中の入力、判定結果、チェック状態は保存しない
- `buildQuestionText`等で生成され、ユーザーが確認した完成質問文だけ保存対象にできる

### QT-SAVE-02 保存操作

- 質問文の出力後に`記録として保存`を表示する
- 保存は任意であり、コピー・共有に必須ではない
- 保存後もコピー・共有を利用できる

### QT-SAVE-03 保存先と既定

既存`notes`を再利用し、破壊的変更を行わない。

- `type`: `thought`
- `tags`: `workplace,workplace_question`
- `visibility`: `private`
- `is_git_candidate`: `false`
- `source`: 既存手動保存の値
- タイトル：日付を含む一般的な名称（例：`質問メモ YYYY-MM-DD`）

守秘既定はカテゴリ初期値より優先する。

### QT-SAVE-04 二重保存

- 保存中の二重実行を防止する
- 保存成功後は同じ出力を重複保存できない状態にする
- 出力を編集・再生成した場合は、新しい内容として保存可能に戻してよい

## 15. 守秘・セキュリティ

- 顧客名、会社名、氏名、内部URL、システム名、認証情報、社内資料本文、会社固有の判断基準を入力しない
- 危険条件は一般化された選択肢のみとし、具体的な対象名を入力させない
- 外部共有前は`33`で定義する現場適応用チェックを必須にする
- 保存時はprivate・Git候補外を強制する
- 公開HTML、家族用、配布用、GitHub出力へ含めない

## 16. エラー・例外処理

- 判定入力が不足していてもクラッシュしない
- 危険条件が未選択でも、未選択を安全と断定しない。選択を促す
- 保存失敗時も完成質問文を維持する
- 質問画面への引き継ぎに失敗した場合は、空フォームを表示し、詰まり記録へ戻れるようにする
- 戻る操作で判定途中データが失われることは許容するが、その旨を仕様上の一時データとして扱う

## 17. アクセシビリティ

- 選択肢は色だけで状態を示さない
- チェック・ラジオ相当の状態を`accessibilityState`で示す
- 判定結果は見出しと文章で示す
- 主要操作は44相当
- 最大フォントサイズでも結果と操作へ到達できる

## 18. 受入条件

判定状況（2026-08-08）：**Gate A〜E すべてPASS**。Gate A・B・C・Eは §21 の実装・確認記録（2026-07-25。判定単体28/28・Web・Android Expo Go）で、Gate DはPhase 16Cと統合したうえで **EAS APK versionCode 10 のPixel最終受入（2026-08-08。下記「EAS APK versionCode 10 最終確認」・`33` §23.7）** で確認した。

### 18.1 Gate A：判定ロジック単体

- [x] 危険条件1件で「すぐ確認する」
- [x] 完全停止10分以上で「早めに質問する」
- [x] 通常20分以上で「早めに質問する」
- [x] 10分未満・低影響・未確認項目ありで「もう少し調査する」
- [x] 複数条件では最も緊急な結果になる
- [x] 現場ルールありで優先注意を表示する

### 18.2 Gate B：画面フロー

- [x] 詰まり記録から判定へ進める
- [x] 判定をスキップして質問文作成へ進める
- [x] 3結果それぞれから質問文作成へ進める
- [x] 詰まり内容・試したことを質問フォームへ引き継げる
- [x] 戻る操作で行き止まりにならない

### 18.3 Gate C：保存

- [x] 判定途中データがDBへ保存されない
- [x] 完成質問文だけ任意保存できる
- [x] `workplace,workplace_question`、private、Git候補falseで保存される
- [x] 保存失敗時に質問文が残る
- [x] 二重保存されない

### 18.4 Gate D：共有・守秘

- [x] 3つの守秘チェック完了前はコピー・共有できない
- [x] 3つの守秘チェック完了後にコピーできる
- [x] 3つの守秘チェック完了後に `ChatGPTなどへ共有`へ進める
- [x] 共有キャンセルで質問文が失われない
- [x] 「送信完了」と誤表示しない

Gate Dの共有・守秘確認は、共通共有基盤（16C-1）と現場適応共有接続（16C-3）の実装後に**統合時に通過させる連携Gate**である。16Bの判定ロジック（16B-1）・判定画面と導線（16B-2）・完成質問文の任意保存（16B-3）は、16Cの共有基盤がなくても実装・確認でき、Gate D（共有）未通過だけを理由に不合格にしない（`33` §17 Gate A〜Gと統合して確認する）。

#### Expo Go統合事前確認（2026-08-05）

Phase 16C（`33`）の実装をmainへ反映した状態（merge commit `b96cc669ddc7c33f3563070d85962a07bb3b85fd`）で、**Pixel＋Expo Go（SDK 54）**による統合事前確認を実施した。

* 質問側の確認項目 **Q1〜Q16 はすべて合格**
* 守秘チェック3項目の有効化条件を確認（3項目完了前はコピー・共有が無効、1〜2項目のみでも無効、3項目完了で有効）
* **本文を実際に変更すると3チェックがすべて解除される**ことを確認
* 解除後はコピー・共有が再び無効になることを確認
* 再度3項目を確認すると共有が再び有効になることを確認
* OS共有をキャンセルした後もMindHubへ戻れ、**質問文が失われない**ことを確認
* 「送信完了」「ChatGPTへ送信しました」等の**誤表示なし**
* 保存結果は **private・Git候補false**（`workplace,workplace_question`）で記録されることを確認
* **保存と共有用守秘チェックは非連動**（保存は守秘チェック必須へ拡張していない）
* **Expo Goでの統合事前確認はPASS**

ただし、**Gate Dの最終合格は、Phase 16全体を含む更新APK（EAS）をPixelへインストールして同等項目を確認した後**に判定する。**この時点（2026-08-05）では Gate D は未通過**であり、チェックボックスも未チェックのままとした。→ その後の最終確認結果は次項を参照する。

#### EAS APK versionCode 10 最終確認（2026-08-08）

EAS preview APK **versionCode 10**（Build ID `10bedf3b-31c3-4fa7-aefe-73f62f960b5e`／対象commit `8706d7d3fcbfa349a1b71c4d45bb65e7e61fbb0a`）をPixelへ上書きインストールし、配布状態で最終確認した（詳細は `33` §23.7）。

* **K17〜K22 PASS**：守秘3チェック未確認時はコピー・共有不可／3項目確認でコピー・共有可能／**本文を変更すると3チェックが全解除**／解除後は再び不可／再確認で再び可能／**コピー成功を実機で確認（K22）**
* **K8・K10・K16 PASS**：質問・報告で守秘3項目確認後にOS共有画面が開く／OS共有キャンセル・Android戻る操作後もMindHubへ戻り**質問文が維持される**／**誤った送信完了表示なし**
* 上記により **Gate D は PASS**（§18.4のチェックを反映済み）
* 利用者向けボタン名 `ChatGPTなどへ共有` は**versionCode 10 の現行仕様**であり、このクローズ作業では変更していない

### 18.5 Gate E：回帰

- [x] 作業開始、詰まり、報告、終業前、翌朝再開を壊していない
- [x] 終業前メモの既存保存・再開導線を壊していない
- [x] WebとAndroidで操作不能にならない
- [x] DBスキーマを変更していない

## 19. Claude Codeへ任せてよい判断

- 判定関数の配置、型名、純粋関数化
- 一時引き継ぎにroute params、context、画面内state等のどれを使うか
- 選択UIの具体的な共通部品
- 質問保存処理を既存`saveWorkplaceNote`相当へ統合する方法
- テストケースの実装方法

任せない判断：

- 10分・20分の変更
- 判定結果の追加・削除
- 危険条件の弱体化
- 判定履歴の保存
- AI自動判定
- 現場ルール本文の保存
- private・Git候補falseの解除
- 判定の強制化

## 20. 実装単位

### 16B-1 判定ロジック

- 入力型
- 3段階判定
- 理由生成
- 単体確認

### 16B-2 判定画面と導線

- 詰まり→判定→質問
- スキップ
- 一時引き継ぎ
- Web幅別確認

### 16B-3 完成質問文の任意保存

- 完成質問文の任意保存のみ（既存notes・`workplace,workplace_question`・private・Git候補false。`14. 完成質問文の保存`）
- 保存の二重実行防止・保存失敗時の質問文維持
- 共有確認画面（`ChatGPTなどへ共有`）への接続・必須守秘チェック・報告文の共有/保存は **16C-3** が担当する（`33` §19）。16B-3では共有処理を実装しない
- Android実機・回帰確認は保存部分について行う

## 21. 実装状況（2026-07-25 追記。未コミット・commit承認待ち）

Phase 16B（16B-1／16B-2／16B-3）を関連1機能としてまとめて実装した。本章 §1〜§20 の製品判断は変更していない（実装状況の記録のみ）。Phase 16Cの共有・OS共有・守秘3チェック・報告文保存は実装していない（境界確認のみ）。

### 実装ファイル
- 新規：`src/features/workplace/questionTiming.ts`（16B-1 判定ロジック・純粋TS）／`src/features/workplace/workplaceHandoff.ts`（一時引き継ぎA/B分離・set/get(純粋読み取り)/clearの3操作＝Strict Mode安全）／`app/workplace/question-timing.tsx`（16B-2 判定画面・`/workplace/question-timing`）
- 変更：`app/_layout.tsx`（Stack.Screen追加・戻るfallback `/workplace/stuck`）／`app/workplace/stuck.tsx`（出力後 `outputAction`「質問タイミングを確認」）／`app/workplace/question.tsx`（引き継ぎread＋`saveQuestionNote` の `onSave`＋`saveLabel="記録として保存"`＋`saveHint`＋`banner`）／`src/components/WorkplaceSceneForm.tsx`（optional `outputAction`・`saveHint`。未指定の既存5場面は挙動不変）／`src/features/workplace/workplaceService.ts`（`saveQuestionNote` 追加。module-private `saveWorkplaceNote` 共用で private・Git候補false・`workplace,workplace_question`・thought・source=manual を強制）／`src/features/workplace/workplaceTags.ts`（古いコメントのみ更新・定数値不変）
- 無変更：DB・schema・依存・`app.json`・`eas.json`・versionCode・SDK。新規ネイティブ依存なし。

### 実装上の設計判断
- 判定ロジックはReact非依存の純粋関数。危険・影響条件は「未回答／該当なし／1件以上」を型（`MultiAnswer`）で区別し、空配列を自動で該当なしにしない（§4.3）。現場ルール優先は判定結果と独立のフラグ（§4.8）。
- 質問タイミング確認画面は入力＋結果を同一1画面・選択式のみ・自由文入力なし・`KeyboardAvoidingView` を追加しない。画面内AppHeaderを持たずStackヘッダーのみ（二重ヘッダーなし）。
- 一時引き継ぎはURL/params/SQLite/ログに本文を載せず、モジュール内一時変数で保持（§13）。set／get（純粋読み取り・副作用なし）／clear の3操作に分け、画面は `useState` 初期化関数で get・初回commit後に `useEffect` で clear する（React Strict Modeで初期化関数が複数回呼ばれても外部状態を削除しない安全構造）。
- 保存は既存 `saveWorkplaceNote` を共用する安全ラッパー `saveQuestionNote` を追加し、呼び出し側から visibility・Git候補・場面タグを変更できない構造（§14・`23` §5.1）。報告文保存は16C-3で同じ内部処理を共用予定。

### 確認結果
- **判定ロジック単体（Gate A）＝合格**：リポジトリ外scratchpadで実モジュールをローカルtscコンパイル→node実行し、§15.2 全ケースを網羅（28/28）。ハーネス・生成物は確認後に完全削除。
- **Strict Mode安全化の再検証（2026-07-25・commit前修正）**：引き継ぎを `consume`（読み取り＋削除）から `get`（純粋）＋`clear` へ分離後、実 `workplaceHandoff.ts`・`questionTiming.ts` をscratchpadで再コンパイル→node実行し、get純粋性（2回getで同値）・set上書き・clear冪等・A/B独立・Strict Mode相当の二重init read（clear前は同値）を含め **45/45合格**（handoff 17＋判定 28）。対象Web回帰24件合格（下記Web確認の「詰まり→判定→戻る→入力変更→再判定で最新入力」「質問再訪で古い値残留なし」「直アクセス残留なし」）。tsc・expo export web・expo-doctor 17/18を再実行し合格。
- **機械確認**：`tsc --noEmit` 合格／`expo export --platform web` 成功（バンドルエラー0）／`expo-doctor` 17/18（失敗1件は既存 `expo 54.0.35` vs `~54.0.36` の無関係パッチ差）／`git diff --check` 問題なし。
- **Web確認（Gate B・Gate C・Gate E）**：6幅横スクロールなし・6入力群到達・二重ヘッダーなし／詰まり→判定→質問の引き継ぎ（確認したいこと→聞きたいこと・状況→背景・エラー→背景末尾・試したこと→試したこと・判定→急ぎ度候補／checked空・decision空・理由は質問へ入らない・URL非露出）／入力変更で結果クリア／3結果再現と各からの質問遷移／investigate次に行うこと最大3／スキップ（入力不足でも可・急ぎ度空・理由なし）／保存（二重保存なし・保存後再保存不可・編集で再保存可・保存ヒントに終業前文言なし・`/notes` 詳細で type=thought/tags=workplace,workplace_question/visibility=private/Git候補false を確認）／回帰（5場面・入口カード不変・他場面に追加操作なし・終業前保存/翌朝再開/戻る維持）／選択状態は☑☐◉○グリフ＋roleで色以外にも判別可／console.error・pageerror・unhandledrejection・VirtualizedList警告0。
- **Gate D（共有・守秘）**：16Cとの連携Gートのため16B単体では対象外（未合格を理由に16Bを不合格にしない）。
- **Android実機（Gate B/C/E のAndroid部分）＝Android Expo Go（Pixel・2026-07-25）確認済み・合格**。**Gate A＝合格／Gate B・C・E＝Web・Android Expo Go合格**（新規ネイティブ依存なし＝EAS APK不要）。EAS APK・iOS・Pixel以外のAndroid機種は引き続き未確認。

### 未確認
- Android Expo Go（詰まり→判定→質問の遷移・6入力群の操作・3結果・スキップ・引き継ぎ・戻る・保存・保存後のコピー・長文と日本語・画面下部操作への到達）。
- EAS APK・iOS・他Android機種。

## 22. UX改善追加（2026-07-25 追記・commit前）

現場適応モードの全画面遷移テスト（Web実測38件・**高重大度問題なし**）で、質問文作成後の完了導線不足と「整理する」後のキーボード残りを確認したため、次の2点を追加した。本章の判定条件・受入条件（§10〜§18）は変更していない。

### 追加内容
1. **整理するでキーボードを閉じる**：`src/components/WorkplaceSceneForm.tsx` の `handleBuild` 冒頭で `Keyboard.dismiss()`。対象＝作業開始/詰まり/質問/報告/終業前（テキスト入力のある5場面共通）。質問タイミング確認画面は選択式のため対象外。入力値・生成内容・保存/コピー処理は無変更（`setTimeout`・自動スクロール・KeyboardAvoidingView変更・依存追加なし）。
2. **質問文作成後の完了導線**：`WorkplaceSceneForm` に optional `completionActions{primary, secondary}` を追加し、**質問文作成画面（`app/workplace/question.tsx`）だけ**に「完了後の移動」（主要＝ホームへ戻る＝`router.dismissTo('/')`／補助＝現場適応へ戻る＝`router.dismissTo('/workplace')`）を表示。中間画面（質問タイミング・詰まり）や重複入口を閉じて戻すため `dismissTo`（expo-router 6.0.24）を使用。outputがある間だけ表示（保存前後・失敗後も維持）・入力変更で非表示・コピー/保存とは別セクション・全幅縦配置・44相当。**保存後の自動遷移はしない**。他4場面には完了導線を追加しない。

### 確認
- `tsc --noEmit` 合格／`expo export --platform web` 成功／`expo-doctor` 17/18（既存パッチ差）／`git diff --check` 問題なし。
- Web（専用ポート8114・Android用8115は維持・確認後8114停止）：追加分34件＋ブラウザ戻り内容確認8件合格。表示条件・ホーム/現場適応へ戻る（フロー/カード/直アクセス）・保存前後の完了導線・保存直後の自動移動なし・保存後コピー可・回帰（他4場面に完了導線なし・stuckの質問タイミングを確認維持・question のコピー/保存維持・終業前保存/翌朝再開・question-timingの戻る維持）・console/pageerror/unhandledrejection 0。ホーム起点フローでのブラウザ戻りで質問/判定/詰まりが復活しないことを内容ベースで確認。
- **Android Expo Go：Pixel実機確認済み・合格（2026-07-25）**（整理する後のキーボード閉／完了導線表示／保存後の自動移動なし／保存後コピー・ホーム・現場適応へ戻る／Androidハードウェア戻りで中間画面が復活しない＝`dismissTo` のネイティブ意味）をすべて実機確認。

### 結果生成後の自動スクロール（2026-07-25 追記・commit前）
「整理する」「判定する」後、結果は同じ画面の下側に表示されるが手動スクロールが必要だったため、明示操作直後だけ結果欄の先頭へ自動スクロールする。判定条件・受入条件は変更していない。

- **整理する**（全5場面・`WorkplaceSceneForm`）：`Keyboard.dismiss()` の後、**出力欄の先頭**へ自動スクロール。
- **判定する**（`question-timing.tsx`）：判定成功時は**結果ブロック先頭**（`siteRulePriority` があれば現場ルール優先注意の先頭、なければ判定結果見出し）へ、入力不足時は**不足項目案内**へ自動スクロール。スキップ（判定せず質問文を作る）は別画面遷移のため対象外。
- **方式**：ScrollViewのref＋出力/結果欄（ScrollViewの直接の子）の `onLayout` で得た content内Y座標＋明示操作直後だけ有効な待ちフラグ。`scrollTo({ y: max(y - 余白, 0), animated:true })`。**初回表示は onLayout＋待ちフラグ／再押下（結果表示済み・入力不変で onLayout が再発火しない場合）は ref 保持したY座標へ `requestAnimationFrame` で1回スクロール（unmount時に `cancelAnimationFrame` で解除）**。**固定setTimeout・measureInWindow・screenY/絶対座標の直接比較・本文長さ推測・レンダーごとのscrollToは不使用**。新規依存なし。
- **自動スクロールしない**：入力変更でoutput/結果が消えるとき・コピー・保存・保存成功/失敗・完了後の移動・戻る復帰・直アクセス・通常再レンダー（待ちフラグを立てないため）。
- **確認**：`tsc`／`expo export web`／`expo-doctor` 17/18／`git diff --check` 問題なし。Web（8114・8115維持・確認後停止）**47件合格**：5場面で出力見出しが上部・横スクロールなし・入力維持／長文で見出し＋本文先頭が上部（末尾に飛ばない）／判定3結果＋現場ルール優先で結果先頭が上部・操作到達／入力不足で不足案内へ・結果非生成・非クラッシュ／入力変更で結果消去＋下方向へ自動スクロールしない・再判定で1回再スクロール／回帰（スキップ・保存・保存後コピー・完了導線・詰まり導線・終業前/翌朝再開）／console・pageerror・unhandledrejection 0。**再押下スクロール修正（2026-07-25・commit前）**：結果表示済みで入力不変のまま再押下すると onLayout 非発火で再スクロールしない問題を確認し、Y座標ref＋rAF方式で修正。Web再確認＝再押下8件＋§6回帰15件すべて合格。**Android Expo Go：Pixel実機確認済み・合格（2026-07-25）**（実機での見え方・長文・繰り返さない・**初回と再押下いずれも移動**・戻る/入力変更で勝手に動かない・下部操作到達をすべて確認）。
