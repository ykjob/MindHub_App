# UI検証チェックリスト（Phase 15）

追加日：2026-07-13

本ファイルは、Phase 15（UI・UX品質改善）における**検証手順・確認項目・合格条件・証跡記録の正本**とする。

## 1. 目的と正本の分担

* `28-ui-ux-quality-improvement.md`：画面別改善・情報設計・UX-01〜UX-16の正本（何を改善するか）
* `29-ui-design-system.md`：デザイントークン・共通UI・状態表示・アクセシビリティ基準の正本（何に従って作るか）
* 本ファイル（`30`）：検証手順・確認項目・合格条件・証跡記録の正本（どう確認し、どう合格判定するか）
* `16-platform-and-distribution.md`：Android APKのビルド・配布・導入・端末運用の正本

本ファイルはAPKのビルド手順・配布手順を記載しない（`16` を参照する）。本ファイルが扱うのは「作成済みAPKを使って、UI・UXをどのように検証するか」である。

## 2. 検証の基本原則

1. 仕様書が完成しただけではPhase 15完了としない（実装・確認・再評価まで含めて完了）
2. Webだけの確認ではAndroid対応完了としない。Androidだけの確認ではWeb対応完了としない
3. 正常系だけでなく、空状態・絞り込み結果なし・エラー・処理中を確認する
4. 見た目だけでなく、操作・戻る・保存・コピー・再試行を確認する
5. UX-01〜UX-16の各項目に合格判定を持たせる（§14）
6. 検証中に別の問題を見つけても、その場で無制限に修正範囲を広げない（§18）
7. 不具合は再現条件と影響範囲を記録し、修正タスクを分ける
8. 各確認には §3 の判定ステータスを使用する
9. **実際に確認していない項目を推測で合格にしない**（未実施は必ず「未確認」とする）

## 3. 判定ステータス

| 判定 | 意味 |
|---|---|
| 未確認 | まだ確認を実施していない。推測で他の判定を付けない |
| 合格 | 確認を実施し、期待どおりであることを確認した |
| 条件付き合格 | 確認を実施し、下記の限定条件をすべて満たす軽微な問題のみが残る |
| 不合格 | 確認を実施し、期待を満たさない問題を確認した。修正タスクが必要 |
| 対象外 | この画面・環境ではその項目が該当しない（理由を記録する） |

**条件付き合格の限定条件**（すべて満たす場合のみ使用できる）。

* 機能は使用可能だが、軽微な視覚問題（微妙なズレ・余白の不揃い等）が残る
* 回避手段があり、今回の必須要件を阻害しない
* 残る問題が後続タスクへ正式に記録されている（`10-tasks.md` または `11-open-issues.md`）
* 安全性・保存・守秘・主要導線には影響しない

**条件付き合格にしてはいけないもの**（該当したら不合格とする）。

* DB保存の失敗・データ消失
* 画面が操作不能になる
* 守秘違反（守秘既定の破壊、守秘注意文の消失、現場情報の公開出力への混入）
* 戻れない（どの経路でもホームへ帰れない）
* クラッシュ

## 4. 検証環境

### 4.1 Web

* PC幅（1024以上。基準は1280）
* タブレット相当幅（768）
* スマートフォン相当幅（360 / 390 / 412 / 480）
* Web直アクセス（URL直打ち・ブックマーク相当。戻る履歴がない状態）
* ページ再読み込み（F5。OPFS競合の自動リロード経路を含む）
* ブラウザ戻る（ブラウザの戻るボタンとアプリ内戻るの整合）
* Consoleエラー確認（開発者ツールで0件を基準とする）

### 4.2 Android

* 実機（versionCode を記録する。エミュレータでの補助確認は可＝`19-android-emulator-testing.md` 参照）
* ステータスバー（重なり・配色）
* Safe Area（上部余白・下部ジェスチャー領域）
* 画面回転の扱い（縦向きを基本とする。横向きは「操作不能・クラッシュしない」ことのみ確認し、レイアウト最適化は対象外）
* ソフトウェアキーボード（表示時に主要操作が使えるか。既知の許容事項は `16` §2.5）
* 戻る操作（システムの戻るジェスチャー／ボタンとアプリ内戻るの整合）
* 文字サイズ変更（端末設定の文字サイズ最大）
* TalkBack（§11の対象画面）
* オフラインまたは通信不安定時（ローカル機能＝メモ作成・保存・閲覧が動くこと。GitHub連携は通信必須のため対象外として扱う）

### 4.3 iOS

iOSはPhase 15の必須完了条件に**含めない**。既存のExpo Go確認履歴（FormFooterBar / InputAccessoryView対応等）を参照する扱いとし、Phase 15での新規確認は行わない。

## 5. 画面幅別確認

基本確認幅：**360 / 390 / 412 / 480 / 768 / 1024**。

すべての画面で6幅を毎回確認するのではなく、次のように効率化する。

| 画面 | 確認幅 |
|---|---|
| ホーム・メモ管理一覧・現場適応入口・プロンプト集（主要4画面） | 6幅すべて |
| 作成・詳細・編集画面（notes / memo / workplace入力） | 360・390・768を基本 |
| 設定 | 390・768を基本 |
| 問題が見つかった画面 | 問題の出た幅の前後（境界幅）を追加確認 |

各幅で最低限確認する項目。

* [ ] 横スクロールが発生しない
* [ ] 文字が途中で不自然に切れない・不自然な位置で改行されない
* [ ] ボタンが画面外へ出ない
* [ ] カード幅が不自然にならない（狭すぎ・広すぎ）
* [ ] ヘッダーが折り返して操作不能にならない
* [ ] FABや固定フッターが内容を隠さない
* [ ] 主要操作が最初の表示範囲で理解できる
* [ ] 画面密度が過剰にならない（要素が詰まりすぎない）

## 6. 共通UI検証

`29` を基準として次を確認する。**まだ実装されていない部品・トークンは「未確認」とする**（本ファイル作成時点ではテーマファイル・共通コンポーネントとも未実装）。

* [ ] 色の役割（`29` §3.1）：成功・注意・エラー・AI色が意味のない場所に使われていない
* [ ] 色以外のラベル併用：状態変化に必ず文字の変化が伴う
* [ ] 余白スケール（`29` §3.2）：改修済み画面がスケール値を参照している
* [ ] 文字スケール（`29` §3.3）：改修済み画面が6段階（＋例外）に収まっている
* [ ] 角丸（`29` §3.4）：ボタン8・カード12・チップ/バッジpill
* [ ] 最小タッチ領域（`29` §3.5）：主要操作44相当
* [ ] AppHeader（`29` §6.1）：§7で詳細確認
* [ ] FilterChip（`29` §6.2）：選択＝塗り＋太字、絞り込み専用（CategorySelectorを置き換えていない、ソート操作と見た目が区別されている）
* [ ] ListStateView（`29` §6.3)：4状態の表示分岐
* [ ] StatusMessage（`29` §6.4）：結果が文字で表示される
* [ ] 非活性表示：非活性色＋押せない見た目＋accessibilityState
* [ ] 処理中表示：saving / copying が分かる
* [ ] 二重操作防止：処理中の連続タップで多重実行されない

## 7. AppHeader検証

対象4画面：ホーム／メモ管理一覧／現場適応モード入口／プロンプト集。

* [ ] Safe Areaが確保される（Android実機）
* [ ] ステータスバーと重ならない
* [ ] タイトルの位置・サイズが4画面で統一される
* [ ] 補助文が必要な幅（360〜480）で破綻しない
* [ ] 戻るボタンが必要な画面だけに表示される（ホームには表示しない）
* [ ] Web直アクセス時に戻るフォールバック（履歴なし→ホームへ）が機能する
* [ ] 右側操作（新規作成・設定等）が押せる
* [ ] 戻る・右側操作のタッチ領域が十分（44相当）
* [ ] Androidの物理／システム戻る操作とアプリ内戻るが矛盾しない
* [ ] ネイティブヘッダーとの二重表示がない（対象4画面は `headerShown: false`、対象外画面はネイティブヘッダーが従来どおり表示される）

**着手条件（`29` §5.4の再掲）**：versionCode 4 APKで次を確認するまで、AppHeaderの実装に着手しない。→ **2026-07-13に実機確認完了・7項目問題なし（§12.2）。着手条件は満たされた。**

* ホーム2×2表示
* `/notes` 上部余白
* Safe Area
* 戻る操作
* ステータスバーとの重なり

## 8. 画面別検証

### 8.1 ホーム

* [ ] MindHub全体の入口として理解できる（初見で主要機能の関係が分かる）
* [ ] クイックメモ・仕事サポート（現場適応）・メモ管理・プロンプト集の優先順位が分かる
* [ ] 設定が主要機能と同格に見えない
* [ ] FlowDockとMindHubの関係が誤解されない（表示名の混在で「別アプリ」「改名途中」に見えない）
* [ ] 最近のメモが最大3件程度の範囲に収まる
* [ ] 既存のメモ導線を失っていない（一覧全体・作成への経路が残る）
* [ ] FABや既存操作が内容と重ならない
* [ ] 大規模ダッシュボード化していない（集計・統計・複合パネルがない。`28` §12）

**Web確認記録（2026-07-14 バッチ2＝ホーム再構成。ヘッドレスChrome・使い捨てブラウザ内DB・検証ツールはセッション用一時ディレクトリに隔離）**：

* 確認幅：**360 / 390 / 412 / 480 / 768 / 1024（6幅すべて）**。全幅で横スクロールなし・カード2×2維持・文字切れなし
* 結果：自動確認176項目すべて合格・**コンソールエラー0件**
* 確認内容：AppHeader（MindHub＋補助文＋右側「設定」小ボタン）／主要機能カード4件の2×2（上段＝すぐメモする・仕事を整理する、下段＝記録を整理する・AIの型を使う。カード幅・高さ過大なし・タップ領域44以上）／「最近の軽量メモ」＝4件作成して初期3件→「すべて表示」でホーム内展開→「3件に戻す」で復帰を実操作／3件以下・0件では切替操作なし／0件時はListStateViewの空表示＋作成誘導／カード4件・設定・FABの遷移（`/memo/create`・`/workplace`・`/notes`・`/prompts`・`/settings`）／メモ項目→既存詳細遷移（本文コピー導線＝DETAIL-01維持）／全件展開時もFABが操作可能で切替ボタンを覆わない
* スクリーンショット：home-360-empty／home-1024-empty／home-390-3items／home-390-expanded（セッション作業領域に保存・目視確認済み。架空データのみ使用）
* 未確認（Web確認時点）：Android実機＝未実施 → **2026-07-14のAPK確認2（versionCode 6）で実施済み（下記）**。取得エラー状態は実際のDB障害を再現していない（エラー分岐とListStateViewのerror表示実装をコード上で確認したのみ・実機でも未再現）

**Android実機確認記録（2026-07-14 APK確認2＝versionCode 6。詳細は§12.1）**：

* 主要カード4枚の2×2表示／4枚が同じ外観で左アクセントの残留なし／カードの左右バランス／設定が主要カードと異なる補助導線として表示／最近の軽量メモが初期最大3件／4件以上のホーム内展開と3件表示への復帰／FABが一覧・展開操作を妨げない／Safe Areaを含む実機表示／カード4件・設定・FABの遷移／アプリ再起動後の既存メモ表示——**すべて問題なし**
* 未確認のまま残る項目：TalkBack・端末文字サイズ最大（ユーザーが確認したと報告していないため未確認のまま）

### 8.2 メモ管理一覧

* [ ] 初期表示でフィルターが画面を占有しすぎない（スマホ幅で一覧が最初の表示範囲に見える）
* [ ] 検索→カテゴリ→追加フィルター→一覧の順序が理解できる
* [ ] FilterChip（絞り込み）とソート操作の意味が区別できる
* [ ] 有効なフィルター条件が確認できる
* [ ] フィルター解除が可能
* [ ] 一覧件数と絞り込み結果が一致する
* [ ] loading・empty・filtered-empty・errorを区別できる
* [ ] 新規作成へ進める
* [ ] 詳細画面へ進める
* [ ] 戻る操作が機能する（通常遷移・直アクセス・再読み込み後のすべて）

**Web確認記録（2026-07-14 バッチ3＝メモ管理一覧改修 NOTES-01〜04。ヘッドレスChrome・使い捨てブラウザ内DB・検証ツールはセッション用一時ディレクトリに隔離・ポート8092で新規サーバー・架空データ3件のみ使用）**：

* 確認幅：**360 / 390 / 412 / 480 / 768 / 1024（6幅すべて）**。全幅で横スクロールなし・AppHeaderと「＋ 新規作成」破綻なし・詳細フィルター初期閉・一覧カードが初期表示範囲に見える・768以上はコンテンツ最大幅720中央寄せ（カード実測幅696）
* 結果：**自動確認87項目すべて合格・コンソールエラー0件**
* 確認内容：常時表示（検索・カテゴリチップ）と折りたたみ（プロジェクト・タグ・Git候補のみ・アーカイブ済みも表示・並び順。初期閉）／開閉動作と折りたたみ後の条件維持・開閉ボタンの絞り込み条件数表示（数えるのはproject・tag・gitOnly・includeArchivedのみ。並び順は数えない＝下記レビュー反映）／共通FilterChip適用（選択＝塗り＋pill）と並び順ボタンの見た目分離（角丸8 vs pill 999・非ブランド背景を計測比較）／並び順切替で先頭カードの実順序変化（更新日順=編集直後のメモ／作成日順=最後に作成したメモ）／適用中要約（キーワード・カテゴリ・プロジェクト・Git候補・アーカイブ含む・作成日順）／「表示条件をリセット」で全条件初期化（開閉状態は維持）／表示件数がカード数と一致（0件・1件・2件・3件）／empty（条件なし0件＝「メモがありません」＋作成誘導）とfiltered-empty（カテゴリのみ・キーワード不一致・複合条件の3経路で「条件に一致するデータがありません」）の区別／新規作成・詳細への遷移／戻る（通常遷移・直アクセス・再読み込み後の3経路すべてホームへ）
* スクリーンショット：notes-390-empty／notes-390-initial／notes-390-expanded／notes-390-filtered-empty／notes-360-layout／notes-1024-layout（セッション作業領域に保存・目視確認済み。架空データのみ使用）
* レビュー反映（2026-07-14 同日・コミット前）：開閉ボタンの件数から並び順（sortBy）を除外（数えるのはproject・tag・gitOnly・includeArchivedのみ。keyword・カテゴリは常時表示のため、並び順はフィルターではないため数えない。並び順の変更は適用中要約にのみ「作成日順」と表示＝維持）。390幅・使い捨てDB・架空データ2件で回帰確認**13項目すべて合格・コンソールエラー0件**（タグ選択で対象メモのみに絞られ要約に「#dev-ref」・件数（1）加算／作成日順へ変更しても件数（1）のまま増えず要約に「作成日順」／リセットでタグ・並び順を含む全条件初期化＝件数バッジ消滅・全タグチップが選択表示へ復帰）
* 未確認：**Android実機（Safe Area・TalkBack・実機幅・キーボード）＝未実施**（次回APK確認で行う。実施済み扱いにしない）。**取得エラーの実表示は未再現**（catch→loadError→ListStateView status="error"→onRetry（reloadKey更新）の接続をコード上で確認したのみ）

### 8.3 現場適応モード入口

* [ ] 開始・作業中・終了の一日の流れが分かる
* [ ] 5場面すべてへ進める
* [ ] コピー専用／保存あり（コピーと保存）の違いが入口で分かる
* [ ] 翌朝再開導線（前回の再開メモ→この続きから作業開始）が分かる
* [ ] 守秘注意が短く理解できる（消えていない・読める長さ）
* [ ] 過度にフォーム一覧に見えない
* [ ] `26-workplace-demo-samples.md` のサンプルで一連の流れ（5場面→終業前保存→翌朝再開の引き継ぎ初期入力）を再現できる
* [ ] 保存対象と保存しない対象が仕様どおり（保存は終業前メモのみ・private・Git候補false。`23` §6.1）

**Web確認記録（2026-07-14 バッチ4＝現場適応入口改善 WORK-01〜04。ヘッドレスChrome・使い捨てブラウザ内DB・検証ツールはセッション用一時ディレクトリに隔離・ポート8094で新規サーバー・`26` §2.5の架空サンプルのみ使用）**：

* 確認幅：**360 / 390 / 412 / 480 / 768 / 1024（6幅すべて）**。全幅で横スクロールなし・AppHeader（タイトル＋独自価値subtitle＋戻る）破綻なし・守秘注意帯が常時読める・768以上はコンテンツ最大幅720中央寄せ
* 結果：**自動確認72項目すべて合格・コンソールエラー0件**（初回70項目中1件FAILは検証スクリプトの誤判定＝emptyヒント文中の「終業前メモ」をindexOfが先に拾ったもの。判定を場面説明文・カードDOM順で取り直して合格を確認。実装の問題ではない）
* 確認内容：3区分（開始→作業中→終了）と場面順序（作業開始→詰まり記録→質問文作成→進捗報告作成→終業前メモ）／動作種別ラベル（「コピーのみ」4件＝brandSoft・「コピーと保存」1件＝successSoft。バッジ背景色を計測確認）／5枚のカード基本外観が同一（背景・枠・角丸を計測比較）／5場面すべての遷移／戻る3経路（通常遷移・直アクセス・再読み込み後ともホームへ）／入口ラベルと実動作の整合（保存導線・保存説明はendのみ、他4場面は「コピーのみです」）／**1日ループ**＝再開メモなしのempty表示→終業前メモへ`26` §2.5サンプル入力→整理→「再開メモとして保存」→入口の「前回の再開メモ」表示（開始区分内・作業開始カードの直前）→「この続きから作業開始」→`/workplace/start?fromRestart=1`→「明日最初にやること」が今日の作業へ・未完了/補足が先に確認することへ引き継がれることを実操作確認／**守秘**＝保存されたのは終業前メモ1件のみ・`/notes` 詳細でvisibility=private・Git候補=対象外・タグ #workplace #workplace_end 維持・一覧カードにGit候補バッジなし
* スクリーンショット：wp-390-empty／wp-390-restart／wp-360-layout／wp-1024-layout（セッション作業領域に保存・目視確認済み。架空データのみ使用）
* 未確認：**Android実機（Safe Area・TalkBack・実機幅・キーボード）＝未実施**（次回APK確認で行う。実施済み扱いにしない）。**再開メモ取得エラーの実表示は未再現**（catch→loadError→ListStateView status="error"→onRetry（reloadKey更新）→再取得の接続をコード上で確認したのみ）。Gate 6は通過扱いにしない

### 8.4 プロンプト集

Phase 15バッチ5（2026-07-14）のWeb検証結果を反映。**同日、入口名・ID対応を本文が行う処理に合わせて修正した後の再確認**：使い捨てブラウザ内DB・ポート8097新規サーバー・ヘッドレスChrome自動確認（6幅360/390/412/480/768/1024）＋機能確認、計63項目すべて合格・コンソールエラー0件。修正後6入口＝問題を整理する／エラー・不具合を整理する／予定・やることを整理する／作業結果をまとめる／引き継ぎを作る／開発作業を依頼・確認する。**Android実機確認は未実施**（Android列は未確認のまま）。

* [x] 状況別入口から目的のプロンプトへ進める（2026-07-14 Web確認：6入口それぞれで期待ID群だけが表示。複数セクションにまたがるplan入口はリストをスクロールして全9件＝time_slot_tasks/google_tasks/google_calendar/daily_priority/one_day_plan/next_one_action/wife_schedule/outing_plan/shopping_memoを確認。重複対応ID（next_one_action／validation_log／worklog_close_summary／worklog）が指定した複数入口に表示。「すべて」で全42件へ復帰）
* [x] 修正後マッピングの確認（2026-07-14 Web確認：design・researchが「開発作業を依頼・確認する」に表示されない／claude_prompt・claude_work_startが同入口に表示される／wife_schedule・outing_plan・shopping_memoが「予定・やることを整理する」に表示される／daily_priorityは「問題を整理する」から外れ「予定・やることを整理する」に表示される／family_manual・household_ruleはどの入口にも登録されず「すべて」・既存セクション・キーワード検索から到達可能＝household_ruleを「家庭内ルール」検索で確認）
* [x] キーワード検索が機能する（2026-07-14 Web確認：「レビュー」でcodex_reviewを名称一致で抽出。検索対象は名称・ID・セクション名のみ維持）
* [x] 状況＋検索がAND条件になる（2026-07-14 Web確認：「開発作業を依頼・確認する」入口＋「レビュー」＝codex_reviewのみ）
* [x] 0件状態を区別する（2026-07-14 Web確認：検索のみ0件＝「検索条件に一致する…」／状況＋検索0件＝「選択した目的と検索条件の両方に…」を文言で区別。「絞り込みを解除」で状況＝すべて／検索語＝空へ同時初期化）※状況のみ0件は各入口に必ず該当プロンプトがあるため実データでは再現不能（メッセージ分岐はコードで確認）
* [x] セクションの展開と折りたたみが機能する（2026-07-14 Web確認：1件展開アコーディオン維持。入口・検索変更でexpandedIdを強制初期化しない）
* [ ] コピーできる（クリップボードの実内容確認が望ましい）→ **Web成功パスは環境制約で未確認**：ヘッドレス／バックグラウンドChromeでは `navigator.clipboard.writeText` がChromeの権限ポリシーで拒否（NotAllowedError、document.hasFocus()=trueでも拒否）。アプリは失敗を握りつぶさず error パスで「コピー失敗」を表示することを確認。コピー対象が `entry.promptBody` である接続はコードで確認。**成功時のクリップボード実内容一致はAndroid実機／通常ブラウザで要確認**
* [x] コピー中の二重操作を防止する（2026-07-14：コピー中は全コピーボタンdisabled＋「コピー中…」＋accessibilityState.disabled。Web上のコピーは即時解決のためdisabled窓は短いが、連続クリックで結果が1つだけ・破綻なしを確認。実装はcopyingId＋finally解除で確認）
* [x] コピー結果を文字で表示する（2026-07-14 Web確認：StatusMessageで「コピー失敗」（error）を表示・成功時は「コピーしました」（success）。連続コピーは最後の操作結果のみ最後のカードに表示。自動消去＝成功2000ms/失敗2500ms・useEffectクリーンアップ）※成功文言「コピーしました」の実表示はコピー成功が必要なため、上記クリップボード制約によりWebでは失敗パスで確認。文言・kind分岐はコードとerror表示で確認
* [x] 既存の42件と生成ロジック（promptHub.ts / generate_prompt_hub.mjs / mobilePrompts.ts / chatgptPrompts.ts / prompts.html）を壊していない（2026-07-14：変更は `app/prompts/index.tsx` のみ。全42件・7セクション・セクション順・件数見出し・badge・noteの表示を確認。tsc合格）

### 8.5 クイックメモとメモ作成系（回帰確認）

Phase 15で大きく変更しない場合も、回帰確認として次を含める。

* [ ] 新規保存（memo・notesとも）
* [ ] 編集
* [ ] 削除またはアーカイブ（確認ダイアログを含む）
* [ ] カテゴリ選択（CategorySelector）
* [ ] キーボード表示時の入力・操作
* [ ] 固定フッター（FormFooterBar）の表示と操作
* [ ] コピー（本文・プロンプト）
* [ ] GitHub候補・アップロード状態の表示（SyncStatusBadge）
* [ ] 画面を閉じた後の一覧反映（作成・編集が一覧に反映される）
* [ ] エラー時の表示（保存失敗が無反応にならない）

### 8.6 設定

* [ ] Webでクラッシュしない
* [ ] GitHubトークン未設定の状態として表示できる
* [ ] Androidでは既存のSecureStore経路を維持している
* [ ] 保存結果が確認できる
* [ ] 主要画面と同じAppHeaderへ無理に変更されていない（設定はネイティブヘッダー維持。`29` §5.1）

### 8.6.1 確認ダイアログのWeb対応（2026-07-15 バッチ6B-1）

確認ダイアログの2実装（`src/utils/dialog.ts` の `confirmDialog`＝Web対応／`src/components/ConfirmDialog.tsx` の `showConfirmDialog`＝`Alert.alert` 直接）を整理。`dialog.ts` を正本（`cancelLabel` 追加）とし、`ConfirmDialog.tsx` は `confirmDialog` へ委譲する互換ラッパーに変更。**さくっとメモ詳細（`app/memo/[id]/index.tsx`）の削除確認**（従来Webで機能不全の可能性）がWebで動作するようになった。設定のトークン削除も `confirmDialog` へ統一。使い捨てブラウザ内DB・ポート8101新規サーバー・ヘッドレスChrome自動確認 **16項目すべて合格・コンソールエラー0件**。※以前 §10・`11` §16・`29` §12に「memo詳細のアーカイブ確認」と誤記していたが、実態は memo詳細の**削除**確認（アーカイブは記録確認詳細＝`notes/[id]` 側で元からWeb対応）。

さくっとメモ削除（`window.confirm` はハーネスで捕捉）：

* [x] キャンセル経路：削除ボタン→確認ダイアログ表示→キャンセル→メモ削除されず詳細に残る→ホームへ戻ってもメモが一覧に残る
* [x] 確認経路：削除ボタン→確認ダイアログ表示→確認→`deleteMemo`→`router.replace('/')` でホームへ→削除したメモが一覧に残らない
* [x] `window.confirm` の表示内容にタイトル「メモを削除」と本文「このメモを削除しますか？この操作は元に戻せません。」の両方が含まれる
* [x] 削除処理（`deleteMemo(db, id)`・`router.replace('/')`）は無変更。多重表示・二重削除なし（`window.confirm` はブロッキング）
* [x] コンソールエラー0件

設定画面（Web）：

* [x] `/settings` が正常表示（GitHub連携設定・「設定を保存」）・クラッシュなし
* [x] WebはGitHubトークン保存非対応（`getToken` がnull＝`hasToken` false）のため**トークン削除ボタンが表示されない**＝これは既存仕様であり不具合ではない。Web確認のためにボタンを強制表示する本番コード変更はしていない
* [x] コンソールエラー0件
* Android/iOSの `Alert.alert` 経路（キャンセル=`style='cancel'`／削除=`style='destructive'`／確認押下時のみ `onConfirm`）は**コード確認のみ**。実機・TalkBackは最終APKで確認（`11` §16）

**Webの制約**：ブラウザ標準 `window.confirm` はボタン文言を変更できないため、Webでは `confirmLabel`/`cancelLabel` は反映されない（title・messageのみ表示。`dialog.ts` にコメントで明記）。

### 8.6.2 さくっとメモの読み込み・保存状態処理（2026-07-15 バッチ6B-2）

対象3画面（`app/memo/create.tsx`・`app/memo/[id]/index.tsx`・`app/memo/[id]/edit.tsx`）に、保存失敗通知（`showMessage`）と読み込みの loading／load-error（再試行）／not-found の区別（共通 `ListStateView` 再利用）を追加。`memoRepository`・`memoService`・保存/取得処理本体・`ListStateView` 本体は無変更。使い捨てブラウザ内DB・ポート8102新規サーバー・ヘッドレスChrome自動確認 **正常系＋該当なし＋6幅＝44項目**、および**getMemoById/runAsync の実例外注入＝29項目**、すべて合格・コンソールエラー0件（注入エラー由来を除く）。例外はテスト時に live DB の1メソッドを一時差し替えて再現（本番コードに失敗分岐を追加していない）。

正常系（12.1）：

* [x] 新規作成→保存中（ボタンdisabled/スピナー）→保存成功→詳細へ遷移→詳細の正常読み込み→編集へ→フォームに本文/カテゴリ反映→編集保存成功→詳細へ戻り本文更新反映

該当なし（12.2）：

* [x] `/memo/<存在しないID>`：「メモが見つかりません」（empty表示。**読み込み失敗表示ではない**）
* [x] `/memo/<存在しないID>/edit`：「メモが見つかりません」・**空の編集フォームを表示しない**（textareaなし）・「← 戻る」表示・タイトル「メモ編集」維持

読み込み失敗（12.3。`getFirstAsync` を注入で例外化）：

* [x] 詳細：ローディングが終了し「読み込みに失敗しました」＋再試行ボタン表示（「メモが見つかりません」ではない）。再試行→復旧して詳細表示
* [x] 編集：同上＋「← 戻る」維持・空フォーム非表示。再試行→フォーム（本文入り）へ復旧
* [x] load-error と not-found を同一表示にしない

保存失敗（12.4。`runAsync` を注入で例外化）：

* [x] 作成：保存中が解除される・「保存できませんでした」表示・入力本文が残る・再度保存操作可能（復旧後は保存成功で詳細へ）
* [x] 編集：同上（本文・カテゴリ維持・再保存で詳細へ戻り更新反映）
* [x] 未処理Promise rejectionが出ない（アップロード後 `reloadMemo` の失敗も握りつぶし・画面全体を落とさない）

幅別（13）：

* [x] 6幅（360/390/412/480/768/1024）で `/memo/create`・実在詳細・実在編集・該当なし詳細・該当なし編集とも横スクロールなし・状態表示は中央で読める・再試行/戻るボタン視認・空フォーム誤表示なし・コンソールエラー0件。390幅で該当なし詳細/編集のスクリーンショット目視

* 未実施：Android実機・TalkBack・文字サイズ最大（最終APK）。今回対象は**さくっとメモ3画面のみ**（記録確認・現場適応・コピー状態・二重操作防止横断は非対象＝STATE-03/04全体は未完了）

### 8.5.1 画面文言・Webヘッダー修正バッチのWeb確認（2026-07-14）

ユーザーのブラウザ確認で見つかった画面文言とWeb戻るボタン表示の修正。使い捨てブラウザ内DB・ポート8098新規サーバー・ヘッドレスChrome自動確認（6幅360/390/412/480/768/1024）＋機能確認、計62項目すべて合格・コンソールエラー0件。**Android実機確認は未実施**。

表示名：

* [x] ホーム主要カード4件が「さくっとメモ／現場適応／記録確認／プロンプト集」（旧ラベルは非表示）。役割説明・表示順・2×2配置は不変
* [x] ホーム「最近のさくっとメモ」見出し・空状態「さくっとメモがありません」＋補助文「…『さくっとメモ』から作成できます」（旧「軽量メモ」「すぐメモする」文言なし）・FAB accessibilityLabel「さくっとメモを作成」
* [x] カード遷移が従来どおり（現場適応→現場適応モード、他）
* [x] 記録一覧画面（`/notes`）のAppHeaderタイトル「記録確認」（旧「メモ管理」なし）
* [x] 現場適応入口の場面カード「行き詰まり記録」・区分説明「行き詰まり、質問、進捗報告を整理します。」・`workplace/stuck` のStackタイトル「行き詰まり記録」・intro「行き詰まったときに…」
* [x] 横スクロールなし・ヘッダー重複なし・コンソールエラー0件（6幅）

Web戻るボタン（当初3画面：`/memo/create`・`/notes/create`・`/settings`）：

* [x] 3画面ともテキスト「← 戻る」（ブランド色）が通常時に視認できる（履歴あり・直アクセスとも。hover・Tabフォーカスでも消えない）。スクリーンショット目視でも確認
* [x] 戻る操作が機能する（履歴あり：`back()`。直アクセス：fallback＝memo/create・settings→ホーム、notes/create→`/notes`）
* [x] 共通 `screenOptions` は無変更・二重ヘッダーなし
* 未実施：Android実機・TalkBack・文字サイズ最大（`11` §16。HEADER-02全体は完了扱いにしない）

### 8.5.2 現場適応5入力画面へのWeb戻るボタン適用（2026-07-14 追加）

同じWebネイティブStackヘッダーの戻るボタン不可視/未描画問題が、現場適応の5入力画面（`/workplace/start`・`/workplace/stuck`・`/workplace/question`・`/workplace/report`・`/workplace/end`）でも確認されたため、同じ `NativeHeaderBackButton` を `headerLeft` として追加適用した（`headerLeft` 対象は計8画面）。5画面の直アクセスfallbackは `/workplace`。ポート8099新規サーバー・使い捨てブラウザ内DB・ヘッドレスChrome自動確認 **133項目すべて合格・コンソールエラー0件**。

* [x] 履歴あり（ホーム→現場適応→各場面）：5画面ともテキスト「← 戻る」が視認（hover・Tabフォーカスでも消えない）・タイトル（作業開始／行き詰まり記録／質問文作成／進捗報告作成／終業前メモ）と重ならない・タイトルが切れない・二重ヘッダーなし・「← 戻る」で現場適応入口（`/workplace`）へ戻る
* [x] 直アクセス（`/workplace/{start,stuck,question,report,end}`）：「← 戻る」表示・押下で `/workplace` へ移動・クラッシュなし・無反応にならない
* [x] `/workplace/start?fromRestart=1`：クエリはロード後も保持・「← 戻る」視認・（直アクセスは）fallbackで `/workplace`。履歴ありの戻るは route/クエリに依存しない `router.back()` で前画面へ戻る（初期入力・引き継ぎ内容・保存/コピー処理は無変更）
* [x] 幅別：5画面すべてを390/1024、タイトルが長めの `/workplace/stuck`・`/workplace/report` を6幅（360/390/412/480/768/1024）で確認（back右端48px・タイトル左端56px＝重なりなし・切れなし・ヘッダー横はみ出しなし・本文レイアウト維持）。390幅で5画面のヘッダーをスクリーンショット保存・目視
* 未実施：Android実機・TalkBack・文字サイズ最大（`11` §16。HEADER-02全体は完了扱いにしない）。この時点で `/memo/[id]`・`/memo/[id]/edit`・`/notes/[id]`・`/notes/[id]/edit` は未変更（直アクセス時に戻るボタン無し）だったが、**その後 §8.5.3（バッチ6A補足）でこの4画面も修正＝`headerLeft` 対象は計12画面**

### 8.5.3 詳細・編集4画面へのWeb戻るボタン適用（2026-07-14 バッチ6A補足）

横断調査（バッチ6A）で「履歴経由が主なので影響は限定的」と評価された `/memo/[id]`・`/memo/[id]/edit`・`/notes/[id]`・`/notes/[id]/edit` を再確認した結果、**当初8画面と同一のネイティブStackヘッダーのマスク画像戻るボタン**を使用しており、**履歴あり遷移でも同じ視認性不安定・直アクセス時は戻るボタン自体が未描画（行き止まり）**を再現（実データのメモ・記録を作成し、ホーム→詳細→編集の履歴経路と実在IDの直アクセスの両方で確認）。**判定＝A（再現）**。既に8画面を修正した方針との整合から、直アクセスの行き止まりを残さず4画面も修正した。同じ `NativeHeaderBackButton` を再利用し `headerLeft` 対象は**計12画面**。使い捨てブラウザ内DB・ポート8100新規サーバー・ヘッドレスChrome自動確認 **53項目すべて合格・コンソールエラー0件**。

fallback方針（詳細＝静的／編集＝対応する詳細への動的ID）：

| 画面 | fallback | 設定場所 |
|---|---|---|
| `/memo/[id]` | `/`（ホーム） | `app/_layout.tsx` の `headerLeft`（静的） |
| `/memo/[id]/edit` | `/memo/[id]`（対応する詳細） | 画面内 `<Stack.Screen options>`＋`useLocalSearchParams` の id（動的） |
| `/notes/[id]` | `/notes` | `app/_layout.tsx` の `headerLeft`（静的） |
| `/notes/[id]/edit` | `/notes/[id]`（対応する詳細） | 画面内 `<Stack.Screen options>`＋id（動的） |

* [x] 履歴あり（ホーム→詳細→編集）：4画面ともテキスト「← 戻る」が視認・タイトル（メモ詳細／メモ編集）と重ならない・二重ヘッダーなし。編集の「← 戻る」で対応する詳細へ、詳細の「← 戻る」でホーム／`/notes` へ戻る
* [x] 直アクセス（実在ID）：4画面とも「← 戻る」表示・押下で指定fallbackへ移動（詳細→`/`・`/notes`、編集→`/memo/<id>`・`/notes/<id>`＝**編集のfallbackに正しいIDが入る**）・クラッシュ/無反応なし
* [x] タイトル維持：`/memo/[id]`・`/notes/[id]`＝メモ詳細、`/memo/[id]/edit`・`/notes/[id]/edit`＝メモ編集（短縮・変更なし。in-component `<Stack.Screen>` でも `_layout` のtitleが保持されることを確認）
* [x] 幅別：4画面を6幅（360/390/412/480/768/1024）で back視認・タイトル非重複・タイトル未クリップ・ヘッダー横はみ出しなし。390幅でスクリーンショット保存・目視
* [x] 既存8画面（`/settings`・`/memo/create`・`/notes/create`・現場適応5画面）は無変更（`_layout` diffで確認）
* 未実施：Android実機・TalkBack・文字サイズ最大（`11` §16。HEADER-02全体は完了扱いにしない）
* **後続対象**（当時）：メモ詳細（`/memo/[id]`）・メモ編集（`/memo/[id]/edit`）の読み込み例外時にローディングが終わらない可能性 → **バッチ6B-2（2026-07-15）で対応済み**（loading/load-error＋再試行/not-found を区別。§8.6.2）

### 8.7 Phase 15最終統合実装バッチ（2026-07-15、未コミット）

残っていた実装項目を1バッチにまとめて実装。対象：記録確認詳細/編集の読み込み状態（`app/notes/[id]/index.tsx`・`app/notes/[id]/edit.tsx`）、現場適応の保存中二重実行防止（`src/components/WorkplaceSceneForm.tsx`）、コピー二重実行防止・結果タイマーのクリーンアップ（共通hook `src/hooks/useCopyFeedback.ts` 新設＋`app/memo/[id]/index.tsx`・`app/notes/[id]/index.tsx`・`NoteForm.tsx`・`WorkplaceSceneForm.tsx` へ適用）、FormFooterBar下部Safe Area（`src/components/FormFooterBar.tsx`）、A11Y属性・44相当タッチ領域・selected/disabled読み上げ（上記＋`CategorySelector.tsx`・`app/memo/create.tsx`・`app/memo/[id]/edit.tsx`・`app/settings.tsx`）。

STATE区別（記録確認詳細・編集。§9の表に沿う）：

* [x] 記録詳細（`notes/[id]`）：`useFocusEffect`＋activeフラグ＋`loadError`/`reloadKey` で loading／load-error（`ListStateView status="error"`＋再試行）／not-found（`status="empty" emptyMessage="メモが見つかりません"`）／loaded を区別。読み込み例外でローディング固着しない。アーカイブ・書き出し後は非致命の `reloadNote`（catch内包）で再取得＝画面全体をエラーに倒さない。既存のアーカイブ/書き出し/本文コピー/プロンプトコピー/編集導線/静的Web戻る（`_layout` の `/notes` fallback）は無変更（コード確認）
* [x] 記録編集（`notes/[id]/edit`）：`useEffect`＋activeフラグ＋`loadError`/`notFound`/`reloadKey`。null→not-found（**空フォームを表示しない**＝loaded時のみ `NoteForm` を描画）、例外→load-error＋再試行。各状態で動的Web戻る（`<Stack.Screen headerLeft>`＝`/notes/${id}` fallback）を維持

二重操作防止・タイマー：

* [x] `useCopyFeedback`：copying中は `run` を無視＋呼び出し側が `disabled`／`accessibilityState.disabled` を反映。成功/失敗の文字表示は従来どおり（本文コピー2000/2500ms、現場適応コピー2000/2000ms、プロンプトコピーは失敗時のみダイアログ＝従来挙動を維持）。結果タイマーIDを保持し、次のコピー時に古いタイマー解除・アンマウント時解除・アンマウント後 setState しない（コード確認）
* [x] 現場適応保存（`WorkplaceSceneForm`）：`savingRef`＋`saveState='saving'` で `await onSave` 中はボタンdisabled・二重実行しない。保存成功（保存しました・以後disabled）/保存失敗（保存失敗・2500ms後idle＝再保存可）は従来表示を維持。失敗タイマーはrefで保持しアンマウント時解除。ルート・保存内容・タグ・private・Git候補外は無変更。**コピー専用4画面（onSave未指定）へ保存処理は追加していない**（コード確認）

A11Y・タッチ領域・selected/disabled：

* [x] 保存/キャンセル/編集/削除/アーカイブ/書き出し/コピー/整理する/GitHubアップロード/GitHubトークン削除/再試行の各操作へ `accessibilityRole="button"`＋`accessibilityLabel`、処理中/入力不足へ `accessibilityState.disabled`（＋保存系は `busy`）を付与。結果表示ボタンに `accessibilityLiveRegion="polite"`。見出しに `accessibilityRole="header"`（コード確認）
* [x] CategorySelector・NoteForm の各chipに `accessibilityState.selected`＋`accessibilityLabel`。色だけで選択・無効を伝えない。表示文言・視覚デザインは維持（コード確認）
* [x] 主要ボタン（保存/キャンセル/コピー/編集/削除/アーカイブ/書き出し/整理/GitHubアップロード）へ `minHeight:44`＋`justifyContent:'center'`。chip類・小ボタン（プレビュー切替・トークン削除）は外観を変えず `hitSlop` でタップ判定を44相当へ拡張（過度に大きいボタンにしない）（コード確認）

FormFooterBar下部Safe Area：

* [x] `useSafeAreaInsets()` を導入し、**通常フッターのみ** `paddingBottom = 12 + insets.bottom`。iOSの `InputAccessoryView`（キーボード直上バー）へは加算しない（重複回避）。Webでは `insets.bottom=0` で従来表示に一致（コード確認・下記バンドル確認）。`InputAccessoryView`/`InteractionManager`/フォーカス処理は無変更

静的・バンドル確認：

* [x] `npx tsc --noEmit` 合格
* [x] `git diff --check` 問題なし（LF/CRLFの通知のみ）
* [x] `npx expo export --platform web` 成功（796モジュール・バンドルエラー0件）＝変更した全画面・新hook・safe-area導入が**Web向けにコンパイル/バンドルできることを確認**。dist（gitignore対象）は確認後削除
* [x] Web文字拡大相当（VISUAL）のコードレビュー：本バッチが変更したのは詳細/編集/フォームの状態分岐・ボタンのminHeight/hitSlop・フッターのpaddingBottomのみで、AppHeader・NativeHeaderBackButton・ホーム2×2カード・記録確認フィルター/chip・プロンプト集chip・現場適応ラベルの**レイアウトは無変更**＝新たな文字拡大崩れを持ち込んでいないことを確認

対話的Web確認（ヘッドレスChrome、2026-07-15 実施。他スレッドに干渉しない専用ポート8114新規サーバー・使い捨てuser-data-dir＝使い捨てブラウザ内DB。puppeteer-coreはリポジトリ外scratchpadに隔離インストール＝package.json/package-lock無変更）。**89項目中85合格・コンソールエラー0件**。失敗4件はいずれもヘッドレスChromeが `navigator.clipboard.writeText` を拒否する環境制約（コピー成功パスが再現できない＝バッチ5と同じ既知事象）で、**アプリはerrorパス「コピーできませんでした」を正しく表示し、結果タイマーで既定ラベルへ戻ることを別途確認**（下記）＝コード欠陥ではない。

* [x] 6幅（360/390/412/480/768/1024）×12ルート（`/`・`/notes`・`/workplace`・`/prompts`・`/settings`・`/memo/create`・`/notes/create`・現場適応5入力画面）＝**72件すべて横スクロールなし**・各ルート読み込み成功
* [x] さくっとメモ：作成→保存→詳細（本文表示）→編集→保存→詳細に更新反映。`/memo/<不正ID>`＝「メモが見つかりません」
* [x] 記録確認：`/notes/create`（NoteForm）→保存→詳細（タイトル表示）。アーカイブ確認ダイアログ発火（`window.confirm` にtitle＋本文）→アーカイブ済みバッジ表示。`/notes/<不正ID>`＝「メモが見つかりません」
* [x] 二重操作防止（現場適応・終業前メモ）：整理する→保存を連続クリック→「保存しました」表示＋保存ボタン `aria-disabled=true`（二重実行なし）
* [x] コピー二重クリック非クラッシュ：memo詳細で本文コピーを連続クリックしても画面が生存（本文表示維持）
* [x] 現場適応コピー専用画面（`/workplace/stuck`）：整理する→コピー操作が動作し、**保存ボタンが存在しない**（onSave未指定）ことを確認
* [x] コピー失敗パス＋タイマー：ヘッドレスでwriteText拒否時に「コピーできませんでした」を表示し、約2.5秒後に既定ラベル「本文をコピー」へ戻る（`useCopyFeedback` の失敗分岐・setTimeout/clearTimeout・アンマウント/マウント判定が機能）
* [x] コンソールエラー0件（clipboard由来のNotAllowedErrorを除く。除外なしでも0件）

未実施（推測で合格にしない）：

* [ ] **コピー成功パスの文字「コピーしました」表示・クリップボード実内容一致**：ヘッドレスChromeのwriteText拒否のため未再現。通常ブラウザ（フォーカスあり）またはAndroid実機で確認する（失敗パスは上記で確認済み）
* [ ] Android実機・TalkBack・文字サイズ最大（最終APK。Gate 6・Gate 7は未完了）

### 8.8 Phase 15 Web全体回帰（2026-07-15、commit前）

§8.7の統合実装に対し、前回未確認・結果未明示だった機能経路をまとめて補完した回帰。他スレッドに干渉しない専用ポート8114新規サーバー・使い捨てuser-data-dir（使い捨てブラウザ内DB）・puppeteer-coreはリポジトリ外scratchpad隔離（他スレッドの8081/8082は無停止・repo無汚染）。読み込み/保存失敗はアプリコード・DBを恒久変更せず、**確認用ハーネスがReact fiber経由でlive DBを取得し `getFirstAsync`（読み込み）／`runAsync`（保存）をテスト中だけ一時差し替えて再現**（本番コードに失敗分岐を追加していない）。

**結果：79項目すべて合格・不合格0・console.error 0・console.warn 0・pageerror 0・unhandledrejection 0**（clipboard由来を除外せずに0）。区別は下記のとおり。

**合格（実際に正常動作を再現）**：

* [x] 記録確認：一覧表示・作成→保存→詳細・編集→保存→詳細に反映・絞り込み（キーワード一致で残る／不一致で「条件に一致するデータがありません」＝filtered-empty／「表示条件をリセット」で一覧復帰）・アーカイブ（`window.confirm` 発火→「アーカイブ済み」バッジ→解除）・不正IDのnot-found（「メモが見つかりません」）
* [x] 記録確認の**詳細の読み込み失敗表示＋再試行**（`getFirstAsync` 注入で例外化→「読み込みに失敗しました」＋再試行→復旧して本文表示）
* [x] 記録確認の**編集の読み込み失敗表示＋再試行**（同注入→edit「読み込みに失敗しました」・**空フォームを表示しない**〔タイトル入力欄なし〕・「戻る」維持→再試行で本文入りフォームへ復旧）
* [x] さくっとメモ：作成→保存→詳細・編集→反映・**保存失敗**（`runAsync` 注入→`window.alert`「保存できませんでした」・**入力本文維持**・作成ルート維持・保存ボタン再活性）→**失敗後の再保存で成功**・不正IDのnot-found・**削除**（`window.confirm`→ホームへ遷移）
* [x] 現場適応：入口→作業開始/詰まり/質問/報告/終業前の5画面導線。終業前メモ＝**保存失敗**（`runAsync` 注入→「保存失敗」・入力維持）→**失敗後の再保存で成功**→保存済みで保存ボタン `aria-disabled=true`（二重実行防止）。コピー専用画面（詰まり）＝**保存ボタン非存在**・コピー失敗表示→約2.2秒後に既定ラベル「コピー」へ復帰・**コピー直後に「戻る」で別画面へ移動してもunhandledrejectionなし**
* [x] その他：ホーム2×2の4カード表示・プロンプト集の状況別chip操作・設定（トークン未設定時は入力欄表示・「設定済み」非表示・「設定を保存」あり・トークン行の横あふれなし）
* [x] Web戻る12画面 ×（直アクセス＋再読み込み）＝24件すべて、`window.confirm`不要でfallbackへ正しく戻り（settings/memo/create/memo詳細→`/`、memo編集→`/memo/<id>`、notes/create/notes詳細→`/notes`、notes編集→`/notes/<id>`、現場適応5画面→`/workplace`）、Unmatched Route・空白画面にならない
* [x] 文字拡大相当（ページズーム1.5）：settings・ホーム・記録確認・メモ作成・終業前メモ・メモ編集で**横スクロールなし**、ネイティブヘッダーの「戻る」とタイトルが**重ならない**（settings/メモ作成/終業前メモ/メモ編集で矩形交差なしを実測）

**環境制約により未確認**：

* [ ] コピー**成功**経路（「コピーしました」表示・クリップボード実内容一致）：ヘッドレスChromeの `clipboard.writeText` 権限制約により成功経路は再現不可。**拒否時の失敗表示・二重操作防止・タイマー復帰・コンソールエラーなしは確認済み**（§8.7＋本節）。通常ブラウザ（フォーカスあり）または最終Android実機で確認する

**未確認（本セッション対象外）**：

* [ ] Android実機・TalkBack・文字サイズ最大（最終APK。Gate 6・Gate 7は未完了）
* [ ] GitHub連携の実通信（設定保存後の実アップロード等）は今回対象外＝完成済みとしない

**不合格（アプリコードの不具合）**：なし（0件）。前回報告の「失敗4件」に相当するのはすべて上記のclipboard環境制約であり、コード欠陥ではない。

補足：記録確認（notes）に**削除UIは存在しない**（アーカイブ／解除のみ。詳細のメッセージも「削除はされません」）。削除はさくっとメモ（memo）の機能で、上記で確認済み。

### 8.9 現場適応フォームのキーボード下部余白（2026-07-15、未コミット）

**発見したAndroid実機不具合**：現場適応モードの入力画面（`WorkplaceSceneForm`）でAndroidキーボードを表示すると下側の入力欄がキーボードに隠れ、下まで十分スクロールできない（特に `/workplace/stuck` の後半フィールド）。原因＝`ScrollView` の `contentContainerStyle` の下部余白が固定値40のみで、キーボード表示中にキーボード高さぶんの余白がないため。

**実装した修正（`src/components/WorkplaceSceneForm.tsx` のみ・共通コンポーネント1か所）**：

* `Keyboard.addListener('keyboardDidShow', e => setKeyboardHeight(e.endCoordinates.height))` ／ `'keyboardDidHide' → setKeyboardHeight(0)`。アンマウント時に両subscriptionを `remove()`
* `contentContainerStyle` を配列化し、`keyboardHeight > 0` のときだけ `{ paddingBottom: 40 + keyboardHeight }` を追加（`styles.content` の `paddingBottom:40` を上書き＝二重加算しない）。非表示時は通常の40のまま
* `keyboardDismissMode="on-drag"` を追加（下スクロールでキーボードを閉じやすくする）
* `KeyboardAvoidingView` への置換・`app.json` の `softwareKeyboardLayoutMode` 変更・固定の大余白追加・画面別5ファイルへの重複実装はしていない。入力項目・保存内容・タグ・private/Git候補外・ルートは無変更

**Androidで確認できた範囲**：**なし（本セッションではAndroid実機／エミュレータが利用できず未実施）**。キーボードの表示・非表示イベントはネイティブのみで発火し、Web（react-native-web）では発火しないため、`/workplace/stuck` のキーボード回避・下部までのスクロール等（必須確認1〜10）は**未確認**。

**Web（回帰確認）**：ヘッドレスChrome（専用ポート8114・使い捨てブラウザ内DB・puppeteerはリポジトリ外scratchpad隔離・他スレッド無停止）で現場適応5画面（start/stuck/question/report/end）を確認。**21項目すべて合格・console.error 0・pageerror 0・unhandledrejection 0**（各画面が開く・入力欄へ入力し値が保持される・横スクロールなし・縦スクロール可・stuckは整理する→出力表示）。Webでは `keyboardHeight` は常に0で下部余白は従来の40のまま＝**Web表示は変化なし**を確認。`npx tsc --noEmit` 合格・`npx expo export --platform web` 成功・`git diff --check` 問題なし。

**未確認の範囲**：Android実機／エミュレータでの必須確認1〜10（キーボード表示中の下側入力欄の可視化・下部までのスクロール・閉じたときの余白復帰・入力保持・整理/コピー/保存ボタンまでの到達）。→ 最終Android実機確認（次回APKまたは端末実機）で確認する。

## 9. 状態別検証

| 状態 | 発生条件 | 期待表示 | 操作可能範囲 | 合格条件 |
|---|---|---|---|---|
| loading | データ取得中 | ActivityIndicator等の読み込み表示 | 取得対象以外の操作は可 | **emptyと同一表示にならない** |
| success | 取得成功・データあり | 一覧・内容の通常表示 | 全操作 | データが正しく表示される |
| empty | 取得成功・データ0件（条件なし） | 「データがない」旨＋作成への誘導 | 作成・戻る | loadingでもerrorでもないことが分かる |
| filtered-empty | 取得成功・絞り込み一致0件 | 「条件に一致しない」旨＋**条件解除方法の表示** | 条件変更・解除 | **emptyと区別できる** |
| error | 取得失敗 | 短い説明＋**再試行方法**＋データが消えたわけではない旨 | 再試行・戻る | **空状態として表示されない** |
| saving | 保存処理中 | ボタンdisabled＋処理中が分かる表示 | 保存以外（入力は不可でよい） | **二重操作を防止する** |
| copying | コピー処理中 | ボタンdisabled（長引く場合） | コピー以外 | 二重操作を防止する |
| copied | コピー成功 | 「コピーしました」等の文字表示 | 全操作 | 文字で結果が分かる。自動で消える場合も読み取れる時間（2秒程度以上）を確保する |
| copy-error | コピー失敗 | 「コピーできませんでした」等の文字表示 | 再コピー | 文字で結果が分かる。読み取れる時間を確保する |
| disabled | 入力不足・処理中等 | 非活性色＋押せない見た目 | その操作以外 | 押しても何も起きない。可能なら理由が近くに示される |

追加ルール。

* loading中にemptyを表示しない（取得完了までempty判定しない）
* errorを空状態として扱わない（catchで握りつぶさない）
* filtered-emptyには条件解除方法を表示する
* errorには再試行方法を表示する
* saving・copying中は二重操作を防止する
* success・errorの結果表示は、一定時間後に消える場合でも読み取れる時間を確保する

## 10. エラーと異常系

* [ ] SQLite読み込み失敗：一覧がerror表示になる（空表示にならない）
* [ ] Web OPFS初回競合：リロード直後の `NoModificationAllowedError` で1回だけ自動リロードして回復する（sessionStorageフラグが残留しない）
* [ ] Web OPFS再読み込み後も失敗した場合（別タブで開いている等）：再リロードループに入らず、エラーが表示される
* [ ] 保存失敗：無反応にならず、失敗が文字で表示される
* [ ] コピー失敗：失敗が文字で表示される（クリップボード権限がない環境等）
* [ ] GitHub設定読み込み失敗：設定画面が落ちず、未設定として表示される
* [ ] WebでSecureStoreを呼ばない：Web実行時に設定画面がクラッシュしない
* [ ] 戻る履歴がない状態：直アクセス・再読み込み後も戻る操作でホームへ帰れる
* [ ] 対象データが削除済みまたは存在しない場合：詳細画面等がクラッシュせず、適切な表示になる
* [x] **ConfirmDialogとWebダイアログの違い** → **解決（2026-07-15 バッチ6B-1。§8.6.1）**：`src/components/ConfirmDialog.tsx`（`Alert.alert` 直接）はRN Webで複数ボタン非対応のため、**さくっとメモ詳細（`memo/[id]`）の削除確認**がWebで機能不全だった（以前ここに「memo詳細のアーカイブ確認」と誤記。memo詳細は削除で、アーカイブは `notes/[id]` 側＝元からWeb対応）。`dialog.ts` の `confirmDialog` を正本とし `ConfirmDialog.tsx` を委譲ラッパー化して修正。Webで削除の確認・キャンセル両経路を確認済み（§8.6.1）

## 11. アクセシビリティ検証

`29` §8の基準を検証項目に変換したもの。

* [ ] 主要操作（ボタン・チップ・戻る・FAB・タップ可能カード）が44×44相当のタッチ領域を持つ
* [ ] TalkBackで主要操作の名称が分かる（何のボタンか読み上げられる）
* [ ] 選択状態が読み上げられる（選択中チップの selected）
* [ ] 非活性状態が読み上げられる（disabled）
* [ ] 処理中状態が読み上げられる（busy。実装した場合）
* [ ] 色を見なくても成功・注意・エラーを判断できる（ラベル文字がある）
* [ ] 端末の文字サイズを最大にしても主要操作が隠れない・押せる
* [ ] 読み上げ順が視覚上の順序と一致する
* [ ] FABが意味のある名称で読み上げられる（「＋」ではなく「メモを作成」等）
* [ ] 戻るボタンが「戻る」として読み上げられる

TalkBack確認対象（`29` §8）。

* ホーム
* メモ管理一覧
* 現場適応モード入口
* プロンプト集
* WorkplaceSceneForm（いずれか1場面で確認すれば共通部品としては足りる。保存ありの終業前メモを推奨）
* メモ作成画面

## 12. Android実機確認

ビルド・配布・インストール手順は `16-platform-and-distribution.md` を正本とする（本ファイルには書かない）。本節は**作成済みAPKでのUI確認項目**のみを扱う。

確認項目。

* [ ] インストール（既存versionCodeからの上書きインストール成功）
* [ ] 起動（開発サーバーなしで起動・クラッシュしない）
* [ ] ステータスバー（重なりなし）
* [ ] Safe Area（上部・下部）
* [ ] ホーム（§8.1＋幅の実機確認）
* [ ] メモ管理（§8.2）
* [ ] 現場適応（§8.3）
* [ ] プロンプト集（§8.4。コピーの実機動作を含む）
* [ ] 設定（§8.6。SecureStore経路）
* [ ] キーボード（表示時に主要操作が使える。既知の許容事項は `16` §2.5を参照し判定に反映）
* [ ] 固定フッター（FormFooterBar）
* [ ] 戻る操作（システム戻ると画面内戻るの両方）
* [ ] 文字サイズ変更（端末設定最大で主要画面が破綻しない）
* [ ] TalkBack（§11）
* [ ] オフライン時のローカル機能（機内モードでメモ作成・保存・閲覧）
* [ ] アプリ再起動後の保存内容（force-stop→再起動でデータが残る）
* [ ] クラッシュの有無（一連の確認を通してクラッシュ0件）

### 12.1 APKビルド確認の時期

**毎タスクごとにAPKをビルドしない**（EASビルドは1回10〜20分かかるため。日常はWeb確認＋各タスクの静的確認で進める）。ビルド確認は次の2回を基本とする。

#### APK確認1

共通UI基盤（テーマ・初期コンポーネント）、ホーム改修、AppHeader対象4画面の基本改修が完了した時点。

* 目的：Safe Area・ヘッダー統一・ホーム再構成の実機妥当性を早期に確認し、後続の画面改修の前提を固める

**確認記録（2026-07-14 ユーザーによるAndroid実機確認。versionCode 5 APK＝バッチ1、対象コミット038173e、build ID a102e935-1c5c-433b-9556-dd8eb4bfb85b）**：

* **大きな問題なし**。起動・主要4画面・AppHeader・戻る操作・既存機能に致命的問題なし
* バッチ1（テーマ＋共通部品＋AppHeader 4画面）はAndroid実機上でも大きな問題なしとして扱う
* この記録はHEADER-02の「Android実機で大きな問題なし」までの記録であり、UX-01〜UX-16の完了判定（§14）は変更しない。Phase 15全体の最終判定は後続レビュー（Gate 6〜7）で行う
* 追加所見：(1) 軽量メモ詳細・メモ管理詳細に本文コピー導線が必要 → DETAIL-01として2026-07-14に実装済み（`10-tasks.md` DETAIL節） (2) Web表示で戻るボタンが見えない所見あり → **切り分けの結果、原因はネイティブStackヘッダーのマスク画像戻るボタンで、直アクセス時は未描画。2026-07-14「画面文言・Webヘッダー修正」バッチで当初3画面（`/memo/create`・`/notes/create`・`/settings`）にテキスト「← 戻る」の `headerLeft`（履歴なしはfallback）を設定してWeb修正済み（§8.5.1）。同日追加で、同じ問題が確認された現場適応5入力画面（`/workplace/start`・`/stuck`・`/question`・`/report`・`/end`。fallback＝`/workplace`）にも適用し `headerLeft` 対象は計8画面に拡大（§8.5.2）。さらにバッチ6A補足で詳細・編集4画面（`/memo/[id]`・`/memo/[id]/edit`・`/notes/[id]`・`/notes/[id]/edit`）でも同問題を再現・修正し**計12画面**（§8.5.3。編集は対応する詳細への動的fallback）。画面内AppHeaderのテキスト戻るは元から視認可。Android実機・TalkBack・HEADER-02全体は未確認のまま（`11` §16）**
* 注記：本確認時点のホームはAppHeader追加のみで、ホーム本格再構成（HOME-01〜04）は未実施

#### APK確認2

Phase 15の画面改修・状態表示・アクセシビリティ・視覚仕上げがすべて完了した時点。

* 目的：§12全項目＋§13回帰＋§11 TalkBackの最終確認（Gate 6）

**確認記録（2026-07-14 ユーザーによるAndroid実機確認。versionCode 6 APK＝バッチ2ホーム再構成＋DETAIL-01、対象コミット6a59f83801e13b8e672d1e9de85e09ac72481f63、build ID 52eb1e4d-d91d-4ccf-a220-aeaa29b43671）**：

* ビルドURL：https://expo.dev/accounts/ykjob/projects/flowdock/builds/52eb1e4d-d91d-4ccf-a220-aeaa29b43671
* 実施時点の注記：本節が本来想定する「全改修完了後の最終確認」より前倒しで、バッチ2（HOME-01〜05＋左アクセント削除）とDETAIL-01まで完了した時点の確認。**Gate 6の通過判定は行わない**（TalkBack等を含む最終確認は後続で実施）
* 確認した操作（すべて問題なし。見た目も問題なし）：
  1. 主要カード4件・設定・FABが正しい画面へ遷移する
  2. 「すべて表示」と「3件に戻す」が正常に動作する
  3. 軽量メモ詳細とメモ管理詳細の「本文をコピー」が正常に動作する（DETAIL-01）
  4. アプリ再起動後も既存メモが正常に表示される
* あわせて問題なしを確認した表示項目：主要カード4枚の2×2表示／4枚が同じ外観で左アクセントの残留なし／カードの左右バランスに違和感なし／設定が主要カードとは異なる補助導線として表示／最近の軽量メモが初期最大3件で表示／4件以上のホーム内展開と3件表示への復帰が正常／FABが一覧や展開操作を妨げない／Safe Areaを含む実機表示に問題なし
* **総合判定：合格（バッチ2ホーム再構成＋DETAIL-01の範囲。§3の判定ステータスによる）**
* 未確認（実施済み扱いにしない）：TalkBack（§11）／端末文字サイズ最大／キーボード／オフライン時のローカル機能／メモ管理一覧・現場適応入口・プロンプト集・設定の画面内容の詳細確認（遷移到達のみ確認）／§13回帰の全項目網羅／取得エラー状態の実表示

#### APK確認3（Phase 15最終APK＝versionCode 7）

Phase 15統合実装（`8a915c2`）＋Web全体回帰まで反映した最終APK。

* 対象APK：versionCode 7、対象コミット `78aeba8d1a448f1d621b4d5cac8ee7d44641a1ad`、build ID `e0352c88-4440-4117-a446-568c9e4cf96e`、profile `preview`（distribution internal・apk）、SDK 54
* ビルドURL：https://expo.dev/accounts/ykjob/projects/flowdock/builds/e0352c88-4440-4117-a446-568c9e4cf96e
* APK URL：https://expo.dev/artifacts/eas/MjW6FN4_bMx_9nUf2F7xzwPxne3gSAm12CJ3TgXwqU0.apk （有効期限 2026-07-28）

**確認記録（2026-07-15 ユーザーによるAndroid実機確認）**：

* **確認済み（発表可能）**：発表で使用する**通常の文字サイズではAndroid実機利用に支障なし**
* **既知課題（合格にしない）**：**端末の最大フォントサイズにすると画面が縦方向へ引き伸ばされ、一部画面で下側が見切れる**。発表には支障がないため、今回は修正せず**発表後の改善課題として保留**（Phase 15後。`11` §16）。※アクセシビリティ上の未完了課題であり、A11Y-04（文字拡大）を合格にはしない
* **延期**：**TalkBack確認は今回実施せず、発表後へ延期**（§11・A11Y-02の読み上げ確認は未実施のまま）
* **未確認**：コピー**成功**経路（「コピーしました」表示・クリップボード実内容一致）は、今回ユーザーからの明確な確認報告がないため**未確認のまま維持**（ヘッドレスWebでは環境制約により未再現。失敗表示・二重操作防止・タイマー復帰・コンソールエラーなしは§8.7・§8.8で確認済み）。GitHub連携の実通信も未確認＝完成扱いにしない
* **総合判定**：**Gate 6・Gate 7は完全合格・完了扱いにしない**（TalkBack未確認＋最大フォントサイズの既知見切れがあるため）。通常文字サイズでの発表可能性（確認済み）と、アクセシビリティ上の未完了課題（最大フォント見切れ・TalkBack）は分けて扱う

### 12.2 Phase 15実装前の基準確認（versionCode 4 APK）

Phase 15の実装に入る前に、現在ビルド済みのversionCode 4 APK（対象コミット6605d43）で次を確認する。

**確認結果（2026-07-13 ユーザーによるAndroid実機確認・7項目とも問題なし）**。

| 項目 | 判定 | 備考 |
|---|---|---|
| ホーム2×2表示（幅480未満のヘッダーグリッド） | 合格 | 実機で問題なし |
| `/notes` の上部余白（insets.top加算後） | 合格 | 実機で問題なし |
| Safe Area（ステータスバーとの位置関係） | 合格 | 実機で問題なし |
| 戻る操作（`/notes` の「← 戻る」タップ） | 合格 | 実機で問題なし |
| ステータスバーとの重なり | 合格 | 重なりなし |
| 横方向の表示崩れ | 合格 | 崩れなし |
| 既存機能への影響（起動・保存・一覧表示） | 合格 | 影響なし |

基準確認は完了し、AppHeader実装の着手条件（`29` §5.4）は満たされた。`/notes` ヘッダー2段化は現時点で不要（上記確認で問題なし。今後問題が出た場合に再判断）。

**追加所見（2026-07-13 実機確認時。単独修正しない）**。

* FlowDockのメモ作成画面（`app/memo/*`）で、保存ボタンとキャンセルボタンが画面下端に近く、見た目に余裕が少ない
* 現時点では単独修正しない。Phase 15の全体仕上げ段階（視覚仕上げ〜APK確認2）で、Safe Area・キーボード表示時の挙動・他の入力画面との統一を含めて確認する
* 具体的な余白値・実装方法は未確定（確認時に判断する。記録先：`11-open-issues.md` §16）

## 13. 既存機能の回帰確認

Phase 15の各段階（特にGate 5・Gate 6）で次の回帰を確認する。

* [ ] `/memo` 系：作成・一覧・詳細・編集・再起動後保持・GitHub関連表示
* [ ] `/notes` 系：作成・一覧・詳細・編集・アーカイブ・検索・絞り込み・並び替え・Markdownプレビュー・コピー・書き出し（Web）
* [ ] `/workplace` の5場面：作業開始・詰まり記録・質問文作成・進捗報告作成・終業前メモ（整理→出力→コピー、終業前のみ保存）＋翌朝再開導線（`26` のサンプルで再演可能）
* [ ] `/prompts`：一覧・検索・展開・コピー成功／失敗表示
* [ ] `/settings`：Web非クラッシュ・保存・読み込み
* [ ] GitHub連携：設定保存・アップロード状態表示（実アップロードは任意）
* [ ] SQLite保存：作成・編集が再起動後も残る（Web・Android両方）
* [ ] Web OPFS：再読み込み・連続リロードで回復する（§10）
* [ ] 公開・家族・配布区分：出力条件（mobileViewPolicy等）に変更が入っていない（コード差分で確認）
* [ ] 守秘既定：現場適応モードの保存が visibility=private / is_git_candidate=false のまま（保存後に `/notes` 側で目視）
* [ ] プロンプト本文と生成ロジック：42件の内容・`npm run generate:prompt-hub` の生成結果が変わっていない

## 14. UX-01〜UX-16 完了判定表

作成時点の判定はすべて「未確認」。Phase 15の進行に合わせて更新する（問題・対応方針の正本は `28` §4・§16）。

| UX ID | 問題 | 主な対象画面 | 実装タスク | Web確認 | Android確認 | a11y確認 | 証跡 | 判定 | 残課題 |
|---|---|---|---|---|---|---|---|---|---|
| UX-01 | ホームが入口に見えない | ホーム | ホーム再構成 | 合格（2026-07-14） | 合格（2026-07-14 APK確認2） | 未確認 | §8.1・§12.1記録・worklog 2026-07-14 | 未確認 | 判定はGate 6〜7 |
| UX-02 | 機能差が分からない | ホーム | 情報設計・名称表示 | ホーム分のみ合格（2026-07-14） | ホーム分のみ合格（2026-07-14 APK確認2） | 未確認 | §8.1・§12.1記録 | 未確認 | 他画面の文言整理は未 |
| UX-03 | 優先順位が見えない | ホーム | 主要機能カード | 合格（2026-07-14） | 合格（2026-07-14 APK確認2。4枚同外観・左右バランス問題なし） | 未確認 | §8.1・§12.1記録 | 未確認 | 表現の十分性はGate 7で再評価 |
| UX-04 | 名称混在 | 全画面 | 表示文言整理 | 未確認 | 未確認 | 未確認 | | 未確認 | app.json変更は時期未確定 |
| UX-05 | ヘッダー混在 | 主要4画面 | AppHeader統一 | 未確認 | 未確認 | 未確認 | | 未確認 | |
| UX-06 | デザイン値分散 | 全画面 | トークン・共通部品 | 未確認 | 未確認 | 未確認 | | 未確認 | 段階移行のため完了範囲を記録 |
| UX-07 | 視覚階層が弱い | 全画面 | 色の意味・カード階層 | 未確認 | 未確認 | 未確認 | | 未確認 | |
| UX-08 | フィルター過多 | メモ管理一覧 | フィルター収納 | 合格（2026-07-14 バッチ3） | 未確認 | 未確認 | §8.2記録 | 未確認 | 折りたたみ式収納。判定はGate 6〜7 |
| UX-09 | チップ曖昧 | メモ管理一覧 | チップ役割分離 | 合格（2026-07-14 バッチ3） | 未確認 | 未確認 | §8.2記録 | 未確認 | 共通FilterChip適用・並び順はボタン分離 |
| UX-10 | フォーム一覧感 | 現場適応入口 | 3区分表示 | 合格（2026-07-14 バッチ4） | 未確認 | 未確認 | §8.3記録 | 未確認 | 判定はGate 6〜7 |
| UX-11 | 保存とコピー不明 | 現場適応入口 | 動作種別表示 | 合格（2026-07-14 バッチ4） | 未確認 | 未確認 | §8.3記録 | 未確認 | ラベルと実動作の整合確認済み |
| UX-12 | 状況別検索なし | プロンプト集 | 状況別入口 | 合格（2026-07-14 バッチ5・修正後63項目） | 未確認 | 未確認 | §8.4記録・worklog 2026-07-14 | 未確認 | 6入口確定（問題を整理する／エラー・不具合を整理する／予定・やることを整理する／作業結果をまとめる／引き継ぎを作る／開発作業を依頼・確認する）。本文が行う処理へ名称・ID対応を修正・固定IDマッピング・検索とAND。コピー成功パスのクリップボード実内容一致はWeb環境制約で未確認（§8.4）。判定はGate 6〜7 |
| UX-13 | エラーと空の混同 | 一覧系画面 | ListStateView適用 | ホーム・メモ管理一覧・現場適応入口（再開メモ） 合格（2026-07-14） | ホームのみ合格（2026-07-14 APK確認2。通常表示のみ） | 未確認 | §8.1・§8.2・§8.3・§12.1記録 | 未確認 | プロンプト集等はSTATE-01。エラー実表示は未再現 |
| UX-14 | 操作性不統一 | 全画面 | タッチ領域・属性 | 未確認 | 未確認 | 未確認 | | 未確認 | |
| UX-15 | スマホ密度 | ホーム・メモ管理 | 幅別確認 | ホーム・メモ管理 合格（2026-07-14・6幅） | ホームのみ合格（2026-07-14 APK確認2・実機表示。メモ管理は未） | 未確認 | §8.1・§8.2・§12.1記録 | 未確認 | メモ管理のAndroid実機は未確認 |
| UX-16 | 独自性が伝わらない | ホーム・現場適応 | 入口再構成・説明文 | ホーム・現場適応入口 合格（2026-07-14） | ホーム側のみ合格（2026-07-14 APK確認2。現場適応入口は未） | 未確認 | §8.1・§8.3・§12.1記録 | 未確認 | Gate 7で判定 |

## 15. 確認ゲート（Gate 1〜7）

各ゲートは、記載の完了条件をすべて満たしたときに通過とする。通過の記録（日付・確認者・証跡）を残す。

### Gate 1：仕様書整備

* `28`・`29`・`30` が存在する
* 3文書の役割に重複がない
* UX-01〜UX-16が追跡可能（§14の表が存在し全件記載）
* 既存仕様書との競合（古い方針）が整理されている（後続の文書整備反映まで含む）

### Gate 2：共通UI基盤

* テーマファイル（`src/theme/index.ts`）が存在し、`29` §3の値と一致する
* 初期共通コンポーネント（AppHeader・FilterChip・ListStateView・StatusMessage）が `29` §6の仕様で存在する
* `npx tsc --noEmit` が合格する
* **対象画面へ一括適用していない**（部品作成タスクの時点で既存画面の差分がない）

### Gate 3：主要画面構造

* ホームがMindHub入口として再構成されている（§8.1）
* AppHeader対象4画面のヘッダーが統一されている（§7）
* メモ管理のフィルター収納・チップ分離が完了している（§8.2）
* 現場適応入口の3区分・動作種別表示が完了している（§8.3）
* プロンプト集の状況別入口が完了している（§8.4）

### Gate 4：状態とアクセシビリティ

* 一覧系画面でloading・empty・filtered-empty・errorが区別される（§9）
* 保存・コピーの二重操作防止が入っている
* 主要操作のタッチ領域が確保されている
* 主要操作に必要なaccessibility属性が付与されている（§11）
* 文字拡大で主要画面が破綻しない

### Gate 5：Web回帰

* 幅別確認（§5）を主要4画面6幅＋他画面基本幅で実施済み
* 直アクセス・再読み込み・ブラウザ戻るを確認済み（§4.1）
* 戻る操作が全経路で機能する
* Consoleエラー0件
* 既存機能の回帰確認（§13）をWebで実施済み

### Gate 6：Android実機

**現状：未完了（2026-07-15 APK確認3時点）**。最終APK（versionCode 7・`78aeba8`・build `e0352c88`）で通常文字サイズのAndroid実機利用は発表に支障なしを確認（§12.1 APK確認3）。ただし**最大フォントサイズで一部画面の下側が見切れる既知課題**があり、**TalkBackは未確認（発表後へ延期）**のため、Gate 6は通過扱いにしない。

* APK確認3を実施済み（§12.1。versionCode 7）
* Safe Area・ステータスバーが正しい（通常文字サイズで確認済み）
* キーボード表示時に主要操作が使える（通常利用で支障なし）
* 戻る操作（システム・画面内）が機能する（通常利用で支障なし）
* [ ] TalkBack確認（§11）＝**未実施（発表後へ延期）**
* [ ] 端末最大フォントサイズ＝**既知課題（一部画面で下側見切れ。発表後の改善課題として保留・合格にしない）**
* 保存が機能し、アプリ再起動後もデータが残る（過去APK確認で確認済み）

### Gate 7：最終再評価

**現状：未完了**。Gate 6が未通過（TalkBack未確認・最大フォント見切れ）であり、コピー成功経路・GitHub実通信も未確認のため、最終再評価・完了判断は確定しない。

* UX-01〜UX-16の再判定が完了している（§14の表がすべて「未確認」以外）
* 4視点レビュー（プロダクト／技術／UI・UX／品質・セキュリティ）を実施済み
* 残課題が区分されている（追加Phase送り・記録済み軽微・対応不要）
* ポートフォリオとしての説明（画面だけで独自価値が伝わるか）を確認済み
* Phase 15の完了判断（十分か、追加Phaseが必要か）が記録されている

## 16. 各タスク終了時の確認

実装タスク1件の終了ごとに、最低限次を実施する。

* `npx tsc --noEmit`
* `git diff --check`
* `git status --short`
* 変更ファイル一覧の報告（意図しないファイルが混ざっていないこと）
* 対象画面のWeb確認（該当する幅・状態）
* 対象画面に隣接する既存機能の回帰確認（保存・遷移・コピー等、触った範囲に応じて）
* Consoleエラー確認（0件）
* 仕様書（`28`・`29`）との一致確認
* 作業ログ（`docs/worklog/current.md`・`current-tasks.md`）の更新

実装対象に応じて必要な追加確認（例：保存系を触ったら再起動後保持、コピー系を触ったらクリップボード実内容）を行う。

## 17. 証跡の記録方法

確認を実施したら、次を記録する（記録先は `docs/worklog/current.md` を基本とし、判定は本ファイル§12.2・§14等の表を更新する）。

* 確認日
* 確認環境（Web＝ブラウザ・ウィンドウ幅／Android＝機種・OSバージョン）
* 対象コミット（ハッシュ）
* 対象APKのversionCode（Android確認時）
* 対象APKのビルドURLまたは識別情報（Android確認時）
* 確認した画面
* 使用した幅
* 確認結果（判定ステータス）
* スクリーンショットの保存先または名称（撮影した場合）
* Consoleエラー数（Web）
* 未確認項目（実施しなかったもの）
* 条件付き合格の理由（該当時。§3の限定条件を満たすことの説明）
* 不具合の再現手順（発見時）
* 次タスク

**スクリーンショット・記録の守秘**：機密情報・GitHubトークン・実データ（実在の現場情報・個人情報を含むメモ）をスクリーンショットへ含めない。確認には架空データ（`26` のサンプル等）を使う（`23` §8.1と整合）。

## 18. 不具合発見時の扱い

* 今回のタスク完了に必須なら、同一タスク内で**最小修正**に留める
* 必須でなければ、新規タスク候補として記録し（`10-tasks.md` または `11-open-issues.md`）、今回は修正しない
* 無関係な問題を同時修正しない（1タスク1目的）
* 再現手順を記録する（環境・幅・操作手順・期待と実際）
* 影響範囲を記録する（他画面・他機能への波及）
* 修正後は、修正前と同じ条件で再確認する
* **守秘・保存（データ消失）・クラッシュ・操作不能に関わる問題は優先度を上げる**（条件付き合格にできない区分。§3）

## 19. Phase 15完了条件

次をすべて満たしたとき、Phase 15を完了とする。

* Gate 1〜7がすべて通過している
* UX-01〜UX-16がすべて判定済み（未確認が残っていない）
* 必須項目に不合格がない
* 条件付き合格の項目が、正式な残課題として `10-tasks.md` または `11-open-issues.md` に記録されている
* WebとAndroidの両方で確認済み（§4・§12）
* 主要機能の回帰確認済み（§13）
* DB・ルート・保存処理・守秘既定を壊していない（`28` §14）
* ポートフォリオとして説明可能（画面だけで独自価値が伝わる状態。Gate 7）
* `current-tasks.md` と `docs/worklog/current.md` が更新されている

## 20. 関連仕様書

* `28-ui-ux-quality-improvement.md`：画面別改善・UX-01〜16の正本（本ファイル§14の問題定義元）
* `29-ui-design-system.md`：共通UI基盤の正本（本ファイル§6〜§9の基準元）
* `16-platform-and-distribution.md`：APKビルド・配布・端末運用の正本（§2.5の初版チェックリストは本ファイル§12と併用）
* `19-android-emulator-testing.md`：エミュレータ・Maestroによる自動確認の調査と雛形（`.maestro/` の2フローは回帰確認の補助として利用可。testID未整備の制約に注意）
* `21-workplace-adaptation-flows-and-ui.md`：現場適応モードのUI正本（§8.3の仕様元）
* `23-workplace-adaptation-security-and-portfolio.md`：守秘の正本（§17の記録守秘・§13の守秘既定確認の基準）
* `26-workplace-demo-samples.md`：現場適応の一連確認・デモ再演用サンプル（§8.3・§13で使用）
* `08-ui-flow.md`：画面構成の横断正本
* `09-roadmap.md`：Phase 15の位置づけ（後続の文書整備で追加）
* `10-tasks.md`：実装タスクと残課題の記録先
* `11-open-issues.md`：未確定事項の記録先
